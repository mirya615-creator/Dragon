# 2026-09-07

- 分析 20 波压力模式两种胜利路径的奖励差异（纯只读分析，未改代码）：
  - 胜利路径 A「20 波内提前胜利」：Tick 中 aiDefeated（对方孵化巢 0 或 Boss InstantDefeat 漏底）→ SettleRun → Victory（波 X 进行中，波 X 不调 CompleteWave）。线上走 `POST /v1/runs/{id}/finish`（result=win，ValidateVictoryFinish 强制 reached_wave<20）。
  - 胜利路径 B「到达 20 波」：EndCurrentWave(20) 先 CompleteWave(20) 再 Victory，或 20 波中途打爆对方基地。线上走 `POST /v1/runs/{id}/victory`（要求 reached_wave=20）。
  - 金币无差异：两种 win 服务器均强制 total_gold==20（ValidateVictorySettlement，VictoryGoldReward=20，广告翻倍 +20 → 40；失败 10、早退 5，见 LocalPlayerGoldGateway）。
  - 符文差异（RuneDropRules，客户端每“完整结束”波 roll，wave≥3，成功上限 4/局）：A 仅 3..X-1 参与；B 全 3..20 共 18 次机会，17-20 波段掉率 40%。波段掉率 3-6:12%/7-12:18%/13-16:28%/17-20:40%；品质同波段表，Epic 25% 整卡。
  - 击杀经验 EnemyRuntime.ExperienceReward：Boss 6/10/15/20（W6/12/16/20，BossExperienceRewards.Get 映射，HeroXpSettlement 判定 hero last-hit）、Elite 3/Normal 1/Swarm 0；局内遥测资源。
  - LocalGameplayRunGateway：ApplyRank/CountCompletedRun/GrantRewards 对任何 Victory/Defeat 均 true（与波数无关）；Go 线上网关这些字段均 false（服务器权威）。

- Main → Greybox_Main「玩法就绪 LoadingPanel」功能（2026-09-07）：
  - 新增 `Assets/Scripts/UI/GameplayLoadingPanelController.cs`（RuntimeInitializeOnLoadMethod(AfterSceneLoad) 自动挂到 Greybox_Main，无需场景手放）。
  - 逻辑：场景激活即把 `Canvas/LoadingPanel`（Greybox_Main 场景已含此节点：全屏 Image + BG 子节点，默认 active=0）SetAsLastSibling + SetActive(true) 盖住棋盘 → 轮询 `DragonBoundBootstrap`（Systems 根）及其 `Match` → 订阅 `MatchController.StateChanged`，State==Running（第一波开始）→ 隐藏；20s 兜底强制隐藏；InitializationFailed 则保留遮罩。
  - 就绪时序（关键）：Bootstrap.InitializeRuntime（Awake，可能 defer 等外部 item/run snapshot 注入）→ Match Ready → 固定停 1s（InitializationPromptSeconds=1，InitializationPromptHidden）→ TwentyWave.StartRun() → EnsureRunning→TryTransition(Running)→BeginWave(1)。隐藏锚点 = MatchState.Running。
  - Main 侧入口不变：StartBtn → MainEnergyController.OnStartClicked → SceneLoader.LoadSceneAsync("Greybox_Main")。SceneLoader 现只为 Login 解析 LoadingImg，Main→Greybox 场景切换黑屏无过渡 UI（可后续扩展通用 fallback）。

- 修订 `GameplayLoadingPanelController` 隐藏锚点（2026-09-07）：原锚 `MatchState.Running` 被否——Greybox_Main 第 1 波实际出兵约在进场 6s 后（Ready 停 1s InitializationPromptSeconds + 波1 FirstSpawnDelay=StartPreparationSeconds=4s，见 TwentyWavePressureConfiguration.CreateCoreLoopV2 / line 84），玩家不希望遮罩盖住这 6s 开局准备期。改为锚 `DragonBoundBootstrap.IsInitialized`（初始化完成棋盘可展示即刻隐藏），并保留 InitializationFailed 保遮罩 / 20s 兜底强制隐藏；不再引用 MatchController/StateChanged。

- 分析 ThunderMage 动画控制台报错（2026-09-07，纯只读未改码）：
  - 报错三连：`PPtr cast failed ... Casting from AnimatorStateTransition to AnimatorState`、`Controller 'ThunderMage': Statemachine for layer 'Base Layer' is missing`、`Animator Controller not valid, Animations will not play`。
  - 排查结论：磁盘上 `Assets/Resources/ThunderMageFinalIdle/Animations/ThunderMage.controller`（minimal 单 Idle 态，无 transition）+ `ThunderMage_Idle.anim`（328 行完整）+ `ThunderMage.prefab` + 三个 `.meta` 全部结构有效、GUID 互相一致（controller.meta=40f6057f3d63b98412040ba88b076020 被 prefab m_Controller 引用；anim.meta=6f752d508be404fde353d2ca345a74ae 被 controller m_Motion 引用）。无 overrideController。
  - 根因：磁盘资源有效但 Unity 报反序列化 cast 错误 → 是 **Library/ArtifactDB 导入缓存里存的该 asset 已损坏**（上一轮 "Corrupted Library Detected" 的残留，重建不彻底）。磁盘文件 OK => 问题在导入缓存而非源文件。
  - 处理建议：彻底关 Unity → 删 `Library` 重建（或 Assets→Reimport All → 不行再删 Library）；重建期间关杀软对该目录实时扫描（防再次锁文件/二次损坏）。若重建后仍报 → 该 .controller 已与当前 Unity 版本不兼容/被截断，需在 Animator 窗口重建或从源重新导出。报错刷屏是因为该 prefab 在 Resources 下被反复实例化/播放。

- ThunderMageFinalIdle 包结构确认（2026-09-07，新导入 Assets/Resources/ThunderMageFinalIdle）：
  - 自包含 2D 部件拼合待机动画包（Unity 2022.3，来源 PSD 9 可见层 1374×1145 PPU100）：Animations/(ThunderMage.controller + ThunderMage_Idle.anim 2s 30FPS loop)、Prefabs/ThunderMage.prefab、Sprites/(Body/Cape/FreeArm/HatBack/HatFront/Head/LowerBody/Staff/StaffArm.png 9 张)、Runtime/ThunderMageIdleSettings.cs（ScriptableObject 参数 duration/breathing/angles）、Settings/IdleSettings.asset、Documentation/README_zh-CN.md。
  - README 官方预览方式：打开 Prefabs/ThunderMage.prefab 预览。玩法接入可 Resources.Load<GameObject>("ThunderMageFinalIdle/Prefabs/ThunderMage")。

- ThunderMage.prefab「corrupted file IDs」根因（2026-09-07，修正为文件本身问题，非 Library/controller 缓存）：
  - Unity Inspector 选中 prefab 报 "Last import generated 1 errors" + "The prefab has corrupted file IDs. This prefab will be unusable"。
  - 用脚本逐层校验：① prefab 内部 fileID 定义/引用完全自洽（无 dangling，每个 anchor 都被引用）；② 9 张 Sprite 外部 guid 全部匹配 Sprites/*.png.meta（Body=2a6b..., Cape=6fda..., FreeArm=864f..., HatBack=bd16..., HatFront=80c8..., Head=df99..., LowerBody=d23b..., Staff=4161..., StaffArm=fe50...）；③ 9 张 png textureType=8（Sprite），可用。三层均正确。
  - 真正根因：prefab 是机器生成的极简 YAML，缺失 Unity 2022.3 反序列化标准字段——对比正常 ItemBg.prefab，每个 GameObject 需 m_CorrespondingSourceObject/m_PrefabInstance/m_PrefabAsset/serializedVersion/m_Layer/m_TagString/m_Icon/m_NavMeshLayer/m_StaticEditorFlags/m_IsActive；Transform 需 m_ConstrainProportionsScale/m_LocalEulerAnglesHint，而 ThunderMage.prefab 全无 → Unity 重建不出对象图 → 判 corrupted file IDs。
  - 修复：新增 `Assets/Editor/ThunderMagePrefabRebuilder.cs`（Menu: Tools > ThunderMage > Rebuild Prefab）——重新加载同 9 张 Sprite + 同 Animator Controller，重建同层级（LowerBody/UpperBodyMotion{Body,HeadMotion{Head,HatMotion{HatBack,HatFront}},StaffArmMotion{StaffArm,StaffGripMotion{Staff}},FreeArmMotion{FreeArm},CapeMotion{Cape}}）的合法 prefab，先备份原文件为 .broken.bak。动画剪辑绑定路径为相对路径（UpperBodyMotion/HeadMotion/HatMotion/CapeMotion/StaffArmMotion/StaffGripMotion/FreeArmMotion），层级一致即可对上。
  - 附：Logs/AssetImportWorker2.log 显示历史上还导入过 ThunderMage-Final-Idle-Standalone 目录和 New Animation.anim（当前已删除，为残留导入记录，不影响）。
  - 编译报错修复（2026-09-07）：`ThunderMagePrefabRebuilder.cs(53,9)/(53,55)` CS0246 `SortingGroup` 找不到 → 该类型在 `UnityEngine.Rendering` 命名空间，脚本顶部补 `using UnityEngine.Rendering;` 即可（脚本已改好）。教训：用到 SortingGroup/SortingLayer 时必须加 `using UnityEngine.Rendering;`。
