# 2026-09-21

## V2 APK 游客登录失败分析（仅分析，未改码）
- 现象：V2 APK 真机 Play as Guest 弹 "Guest login failed. Please try again."。
- 定位：该文案 = `LoginController.cs:268-271` 通用 `catch (Exception)` 分支（网络错误会显示 "Network unavailable..."，服务器业务错误显示服务器 message）→ 排除网络/服务端业务错。
- 服务端正常：从开发机 curl POST .221:8081/v1/auth/guest 返回 201+session。服务器在 192.168.31.221，开发机本机是 192.168.31.34。
- 最大嫌疑：`PersistentAuthSessionStore.Set`（真机走 Java `SecureSessionStore` AndroidKeyStore AES/GCM 写 SharedPreferences）失败 → 抛 InvalidOperationException("Unable to save the authentication session securely.")；编辑器走 PlayerPrefs 分支永不失败 → 解释"编辑器好、真机炸"。`SecureSessionStore.write` catch 静默 return false。
- 明文 HTTP 已由 `DragonBoundNetwork.androidlib` network security config 放行 192.168.31.221/43.166.253.24，非问题。
- 确认手段：服务端访问日志看 POST 是否到达；adb logcat 看 Debug.LogException 堆栈与 "Unable to write the secure authentication session"。
- 建议修复（未执行）：WriteSerializedSession 失败降级回退 PlayerPrefs，勿抛异常阻断登录。

## 真机定位结论（adb 实锤，推翻上面猜测）
- adb 路径：`D:\2022.3.62f3c1\Editor\Data\PlaybackEngines\AndroidPlayer\SDK\platform-tools\adb.exe`（不在 PATH）；设备 `3B15AF0102T00000`（PLJ110）。
- **根因：手机 Wi-Fi 关闭（`settings get global wifi_on = 0`，wlan0 无 IP），走 5G cmnet，IP=10.160.18.249/32，默认路由走 ccmni4** → 无法访问配置里的局域网私有地址 `192.168.31.221:8081`（ping 100% 丢包，TCP nc 超时）。服务端本身正常（PC 侧 curl 返回 201）。
- 公网 `43.166.253.24:8081` PC 侧返回 **502**，说明 Go 服务未真正暴露公网，不能靠改公网 IP 绕过。
- 修复优先级：① 手机开 Wi-Fi 连回 "Mireya.."（免重包）；② 移动端验证需公网：服务端放行 8081 + 改 apiBaseUrl 后重打包；③ USB 调试法：PC `netsh portproxy` 转发 8081→.221 + `adb reverse tcp:8081 tcp:8081` + 配置改 `http://127.0.0.1:8081` 重打包。
## 最终实锤（logcat，11:31）
- 手机连上 Wi-Fi "欢游5g"（192.168.31.92/24）后 ping/nc 全通，但登录仍失败。logcat 实锤：
  `E Unity: Non-secure HTTP connections disabled in release builds` + `InvalidOperationException: Insecure connection not allowed`（栈：UnityWebRequestUnaryTransport.SendAsync）。
- **根因：Unity Release 构建禁明文 HTTP**。`ProjectSettings.asset` 的 `insecureHttpOption: 1`（= 仅 Development 构建允许）；APK 是 Release（logcat `Build type 'Release'`，IL2CPP）。Android networkSecurityConfig 放行 .221 没用——Unity 层先拦。
- **已改**：`ProjectSettings.asset:967` `insecureHttpOption: 1→2`（Always allowed）。待用户重新打包 V2 + 安装验证。
- 旁证：启动时走 `RefreshRestoredSessionAsync`（有残留 session），SecureSessionStore 读正常，无 keystore 报错；上午"Set 失败"猜想被推翻。
- 教训：真机登录问题先用 adb 查 `settings get global wifi_on` / `ifconfig` / `dumpsys connectivity` 确认手机网络，再抓 logcat 看真实异常，最后才怀疑代码。
- 诊断技巧：Unity Debug.Log 在 logcat 的 tag 是 `Unity`（宽列格式 `10367 18397 I Unity   :`，进程号后无 PID/PID 对齐空格差异，用 `grep " Unity *:"`）；Oppo HansManager 会冻结后台 app。
- 手机装有 Clash Meta（com.github.metacubex.clash.meta）但当前无活动 TUN，非本次因素。

## 登录成功 + 重置账号（换新 guest 号）
- **账号绑定规则（实测）**：同一 `device_id` 重复 POST `/v1/auth/guest` → 返回**同一** `player_id`，仅 `is_new_player` 由 true 变 false。**要换新账号必须换 device_id**。
- device_id 存在 `PlayerPrefs["dragonbound.guest.device-id"]`（`GuestIdentityService.cs:11`，32 字节随机 base64url）。**客户端无任何"注销/清除账号"入口**（全仓无 SignOut/DeleteAccount 调用，只有 `RefreshingUnaryTransport` 401 时 `Clear()`）。
- Android 存储位置：session = SharedPreferences `dragonbound_secure_auth.xml`（Keystore AES/GCM 加密，`SecureSessionStore.java` PREFERENCES 常量）；其余全部在 Unity PlayerPrefs xml。旧 AndroidKeyStore alias 与旧 device_id 一起作废，不影响新号。
- **Release 包非 debuggable**（`ro.debuggable=0`，`dumpsys` 无 DEBUGGABLE flag）→ `run-as` 不可用 → **无法用 adb 单删某个 key**，只能 `adb shell pm clear <pkg>`（= 设置里"清除数据"，会杀进程）或卸载重装。
- 注意 `flags=[... ALLOW_BACKUP]`：卸载重装时系统可能从云端恢复 PlayerPrefs → device_id 又回来；需在本机 Android 备份设置里关掉或选"不恢复"。
- 本地数据按 playerId 分 key（签到 `dragonbound.signin.v1.<hash>`、金币/段位、头像 avatarId），换新号自动全新，无需单独清理；但**开发者覆盖项**（Rune `AccountDayOverrideKey`）也被清，需重设。

## InputReceiver 手改被覆盖根因（仅分析）
- InputReceiver 是**代码拥有的运行时控件**（透明点击层），不是自由美术节点。写入者：`GridCellView.EnsureInputReceiver()`（`GridCellView.cs:356-390`）强制 `anchorMin=0,0 / anchorMax=1,1`、`offsetMin=(6,6)/offsetMax=(-6,-6)`（即 Inspector Left/Bottom/Right/Top=6）、`raycastTarget=true`、`SetAsFirstSibling`；常量 `InputInsetPixels=6f`（:12）。契约还被测试锁死：`BootstrapPlayModeTests.cs:901-902`。
- 覆盖触发：`FixedBoardCanvasView` 标 **[ExecuteAlways]**（:15），编辑器下 `OnEnable`/`OnValidate`（force=true）与 `Update`（screenSize/CellSize/RiverGap 变化时）都走 `RefreshEditorPreviewIfNeeded` → `RebuildAuthoredLookups`（:902）→ 每格 `BindAuthoredFixedBoardDefinition` → `EnsureInputReceiver` 重写；运行时 `CreateCells`（Instantiate+Configure+ApplyFixedBoardDefinition，:622 附近）同样重写。→ 重开场景/进 Play/改 Game 视图分辨率/重编译都会把手改值打回 6。
- 正确改法：改 `InputInsetPixels` 常量（全局生效）+ 同步改测试断言；`DraggableUnitView.EnsureInputReceiver`（:169）是另一同名节点（居中 100×100 `InputSizePixels`），同样代码拥有。

## 排行榜 RankText 删内容仍出现（仅分析）
- RankText 文字是运行时代码写入，非静态美术：`MainLeaderboardController.cs:70` 绑定 `Bg/MyLeaderItemBg` 下 `FindUi("RankText")` 的 TMP_Text；OnEnable（:119，每次面板激活）→ SelectPeriod(Weekly) → 拉数据 → `RenderLocalPlayer`（:374）→ `:398-400` 写段位名（`RankProgressionRules.GetDisplayName`，RankLevel≥10 用 RankName）；无数据写空+“0”（:389-390）。列表行 `:325/:328` `SetText(FindUi("RankText"), rankName)` + 星数。
- 删整个节点也不行：`:101` 校验要求 myRankText/myTotalStarsText 非空，缺了在 `:109` 抛 InvalidOperationException（"requires ... RankText/StarAct"）。V2 想隐藏只能代码按 `UiAssets.Active.VariantId` SetActive(false)（连同 RankText/StarAct）。
- 后续需求（只删文字留节点）：方案A=代码置空（Awake 里缓存 `suppressRankText = (UiAssets.Active?.VariantId ?? "V1") == "V2"`，新增 ApplyMyRankText() 替换 :389/:398 写入）；方案B=零代码，V2 场景把 RankText 的 Color alpha 设 0（`SetText`/`.text=` 只改 text 不动 alpha，见 :451）。变体字符串比较用 `"V2"`（`PlayerAvatarProfile.cs:57` 有先例）。均未执行，等确认。
## 选中框/连线"只显示 BeachSelect"排查（仅分析）
- 关键结构：`GreyboxBoardView.UpdateDraggedUnit`（:926）里 **BoardSelect 与 SlectRoad 被同一个 if 门控**（:943-947 `TryGetStableDragPositionAt && drag.CanPreviewTarget && cells.TryGetValue`），且 `dragArrowPreview.Show()`（:954）**先于** `ShowBoardSelect`（:955）执行 → Show 内抛异常会连 BoardSelect 一起不显示；而 BeachSelect 在拖拽开始 `ShowDragSourceSelection`（:905/:922）就显示了，不受该门控 → 完美吻合"只有 BeachSelect"。
- 推论：BeachSelect 能显示 ⇒ `EnsureSelectionSprites`（:2473）已成功加载 BeachSelect **和** BoardSelect 两张图（任一 null 直接 throw，BeachSelect 也出不来）→ 排除"资源缺失/注册表缺 key"。
- 余下嫌疑：① `CanPreviewTarget=false`（`DragPlacementController.IsTargetAccepted`：target==origin / `!board.IsPlaceable`（Locked 格）/ 转移不允许 / 占用格无法解析）；② `TryGetPositionAt` 未命中（V2 格子 RectTransform 被改动/布局不符）；③ `dragArrowPreview==null` 早退（V2 场景 :20368/:38480 已接线，基本排除）；④ Show 内异常（SlectRoad 加载失败 throw :123、`Sprite.Create` 裁剪越界、`Instantiate(shaft)` 空引用）。
- 实现要点：`DragArrowPreviewView` 现为**段池**（`shaft` 被 `SetActive(false)`，真正显示的是 `SlectRoadSegment` 克隆体，`EnsureSegmentCount`/`CreateSegment` :157-180，父节点 = shaft 的父）→ 运行时看不到线时要查 ART_DragArrow 下有没有 SlectRoadSegment，而不是看 ART_DragArrowShaft。
- **用户最终约束：只改 V2，V1 逻辑必须原样** → 必须用运行时变体判断，禁止改共享常量/共享节点；`UiAssets.Active` 为 null（编辑器未激活注册表）时兜底 "V1"，保证 V1 场景预览不受影响。
- **用户反馈"没有报错"后的进一步收敛（重要）**：
  - 无报错 ⇒ 排除 ②之外的资源/异常类：SlectRoad 加载失败会 throw（:123）、`EnsureSelectionSprites` 缺图会 throw（:2488）、Show 内异常会 LogException，全都没有 → 确定是**门控未通过**（else 分支 `HideDragArrow()` + 不调 `ShowBoardSelect`）。
  - **第三个条件 `cells.TryGetValue(target)` 恒真**（target 由遍历 `cells` 得出，:2644 也已校验过 stableDragTarget 在 cells 内）→ 只剩两条：A `TryGetStableDragPositionAt` false（指针没落在任何 cellRect 内）；B `drag.CanPreviewTarget` false。
  - B 的四子条件（`DragPlacementController.IsTargetAccepted` :200-224）：`target == activeOrigin`（还在起点格，属正常）/ `!board.IsPlaceable`（`BoardGrid.cs:265`，Locked 格）/ `!IsTransitionAllowed`（:226 只允许 Bench→Battle|Bench、Battle→Battle|Bench，**Locked 一律不允许**）/ 目标被占用且 `CanResolveOccupiedDrop` false（等级不同不可合成，属设计行为）。
  - 已排除项（实测）：V1/V2 Canvas 完全一致（RenderMode 0 Overlay、`m_Camera:0`、Scaler 1080×1920 Match 0.5）；V2 场景 `dragArrowPreview` 两处已接线（:21914/:34131）；`ART_DragArrowShaft` Image color = (0.96,0.84,0.28,0.94) 非透明 → 段克隆可见；`Initialize(board, destination)` 双参调用（`GreyboxBattlefieldSideView.cs:118`）⇒ `occupiedDropResolver` 非 null。
  - 判定手段：在 `UpdateDraggedUnit` 早退块之后（:942 前）插一行 Debug.Log 打印 hit/target/canPreview/cellType/placeable/occupant；或零代码用**铲子拖拽**（`UpdateShovelDragPreview` 只要求 `TryGetPositionAt` + Locked + 可解锁，**不经过 CanPreviewTarget**）——铲子能显示连线/落点框 ⇒ A 通过、问题在 B。
- **【实锤根因】拖拽连线 NRE（V1/V2 都炸）：`DragArrowPreviewView.Show` 内 `tiledPathSprite.rect` NullReferenceException**
  - 用户 Console 截图：每帧 NRE，堆栈 `DraggableUnitView.OnDrag(:873) → GreyboxBoardView.UpdateDraggedUnit(:954) → DragArrowPreviewView.Show(:58)`。
  - 因果链：① 场景里 `ART_DragArrow` **`m_IsActive: 0`**（V1/V2 都是，实测）→ 组件 inactive ⇒ **`Awake()` 永不执行**；② `Configure()` 只在**编辑器工具** `Editor/DragonBoundPortraitUiBuilder.cs:418` 生成场景时调用过一次，`tiledPathSprite` 是**非序列化运行时字段**（`Sprite.Create` 结果也存不进场景，实测 V2 shaft `m_Sprite: {fileID: 0}`）⇒ 运行时它是 null；③ 首次 `Show()` 在 :68-71 解引用 `tiledPathSprite.rect` → NRE；④ `Show`（:954）先于 `ShowBoardSelect`（:955）→ 异常冒泡 ⇒ **BoardSelect 也不显示**（"一尸两命"）；BeachSelect 走 BeginDrag 路径不受影响。历史呼应：之前 V2"黄条"就是 `shaft.sprite=null` 白块×黄 tint；改段池引入 `tiledPathSprite` 解引用后从"看不见"变"NRE"。
  - **修复已执行**（2026-09-21）：`Assets/DragonBound/Runtime/Presentation/DragArrowPreviewView.cs` 的 `Show()` 内，parent null 检查之后新增懒初始化块（现 :53-65）：`if (tiledPathSprite == null) { ApplyAuthoredPathSprite(); if (tiledPathSprite == null) { Hide(); return; } }`。仅此一处，V1/V2 共用生效，未动场景/注册表；资源缺失仍走 `ApplyAuthoredPathSprite` 的显式 throw（:123）。后续若连线仍偏细，调 `SegmentHeight`（:20，当前 10）而非改场景。
- **【第二层根因】懒初始化后暴露 `ArgumentException: Could not create sprite (1, 91.02675, 98, 9) from a 100x100 texture`（ApplyAuthoredPathSprite 内 Sprite.Create）**
  - 实测：V1、V2 两张 SlectRoad.png 的 meta 均 **`spriteMeshType: 1`（Tight）** → Unity 把 sprite trim 到线条包围盒，`textureRect` **已经不是整图**（V2 实际 ≈(0, 44.03, 98, 9)，正是"线底 47、高 9"的 trim 结果）。
  - 代码 bug：`crop = new Rect(textureRect.x + V2LineX, textureRect.y + V2LineYBottom, …)` 把"整图坐标"常量叠加到已 trim 的 textureRect 上 → y=44.03+47=91.03，+9=**100.03 > 100** 越界 → ArgumentException；V1 同理（trim rect+607 恰好压线 1254 不越界但**裁错区域**，这就是 V1 一直"看不见线"的真正原因）。
  - 修复方向（未执行）：crop 改为 `pathSprite.textureRect` 本身（trim 后即是线条区域），仅当 textureRect≈整图（FullRect 导入）时才回退到 V1/V2 常量偏移；防御性保证 rect 完全落在纹理内。
  - **第二层修复已执行**（2026-09-21）：`ApplyAuthoredPathSprite()`（现 :141-171）改为 `isTrimmed`（textureRect 宽或高 < 纹理 90%）判定——Tight 图直接 `crop = textureRect`；FullRect 图才走 V1/V2 常量分支；末尾加 clamp 保证 rect 不越纹理边界。通用规则：**SlectRoad 类"细线+大片透明"图若导入为 Tight，textureRect 已是线条包围盒，不要再叠加整图坐标常量**（该项目 V1/V2 两张图 meta 都是 `spriteMeshType: 1`）。V1 此前"线看不见"的真正原因是裁错区域（trim rect+607 恰好压线不越界但裁到图顶透明带），不是"线太细"。
- **【第三层】连线显示成功后用户反馈两问题（2026-09-21 14:15，仅分析未执行）**：
  - ① V2 线颜色不是原图色：`CreateSegment()`（:204）`Instantiate(shaft)` 继承场景 authored 黄 tint (0.96,0.84,0.28,0.94)；V2 原图 teal (60,146,175) × 黄 = 绿。修：`segment.color = Color.white`（显示原图色；V1 原图本就是金黄，白 tint 才是"原色"）。
  - ② 线终点超出目标格中心：段池铺设总长 = (count-1)×step + 整段宽，最后一整段永远完整画出；V2 每段宽 ≈108.9（98/9×10），溢出最多约 113 本地单位（大半格）。V1 同病（段宽 ≈168.8）。修：箭头根节点 `AddComponent<RectMask2D>()`（根 rect 已是 (distance, SegmentHeight)），精确裁到目标点，图案不压扁。终点坐标本身没错（`Show` 用 `targetCell.ContentAnchor.position`）。
  - 已输出具体方案（未执行）：`CreateSegment()` 加 `segment.color = Color.white`（若要 V1 保持黄 tint 则用 `UiAssets.Active?.VariantId=="V2"?white:shaft.color`）；`Show()` 懒初始化块内 `GetComponent<RectMask2D>() ?? gameObject.AddComponent<RectMask2D>()`（**不能放 Awake**，ART_DragArrow inactive）。备选＝按 `distance-i*step` 截短最后一段（会压扁末端图案）。粗细调 `SegmentHeight`（:20）。
  - **改动 1+2 已执行**（2026-09-21 14:21）：新增字段 `private RectMask2D pathMask;`；`Show()` 懒初始化块后新增 mask 懒创建（:68-78）；`CreateSegment()` 加 `segment.color = Color.white`。段挂在 shaft 的父 = ART_DragArrow 下，故被 root 的 RectMask2D 正确裁剪。
- **【"时而显示"定性】（2026-09-21 14:26，仅分析）**：非渲染 bug，是 `UpdateDraggedUnit` 门控（:943/:969）随指针移动开合——设计行为。
  - **纠正 14:26 的判据**：铲子路径 **也有**自己的门控（`UpdateShovelDragPreview` :2344-2350 要求 `TryGetPositionAt` + `type==Locked` + `Layout.IsUnlockable(target, side)`；`FixedBoardLayoutDefinition.cs:317-322` 要求 role=Deployment + owner=本方 + `DeployState==LockedUnlockable`）→ **"铲子也时有时无"不能证明是命中链问题**，原判据作废。
  - 正确零代码判据：① 线消失时 **BoardSelect 框是否也同时消失**（Show :954 与 ShowBoardSelect :955 是同一 if 内相邻两行，必须同步；不同步 ⇒ 渲染/RectMask2D 裁剪问题）；② **同一格内小幅移动**是否还闪（闪 ⇒ 命中/RectTransform 错位，不闪 ⇒ 格子合法性决定）。
- **V1 SlectRoad 像素实测（脚本解码 alpha，2026-09-21）**：1254×1254 图中可见像素仅 y=600..646（47 行）x=214..1038；最厚行 y=621 上有 **8 段独立色块**（各约 96px 宽、段间约 12px 空隙）→ 是**菱形虚线链**而非实线；行实心像素 448→748 说明两端尖中间厚。段高 10 时每段内菱形仅约 21×10 画布像素（屏幕约 9×4.3）→ "太细"。
  - **方案 A 已执行**（2026-09-21 14:46）：新增常量 `V1SegmentHeight=22f` / `V2SegmentHeight=10f` + 实例字段 `private float segmentHeight = V1SegmentHeight;`（默认 V1，`UiAssets.Active==null` 时兜底不变）；`ApplyAuthoredPathSprite()` 内按 `string.Equals(UiAssets.Active?.VariantId ?? "V1", "V2", System.StringComparison.Ordinal)` 选值；三处 `SegmentHeight` → `segmentHeight`（段宽计算 :92、`rect.sizeDelta` :99、段 `sizeDelta` :118）。V1 屏幕粗细 4.3px→约 9.5px，**V2 观感零变化**。未做 B（切平菱形）/C（连续实线）；`TiledPathHeight`(:11) 仍为死常量未删。
- **【实锤：RectMask2D 回归 bug，已修复】** 用户实测"框在、线没了" ⇒ Show 走到最后（未 return、未抛异常），是渲染被裁。根因：**RectMask2D 不能用在带旋转的 RectTransform 上**——Unity 用 rect 两个对角点在画布空间的坐标差构造裁剪矩形，宽=distance×cosθ、高=distance×sinθ；拖向下方/左侧（cosθ≤0 或 sinθ<0）得到退化/负尺寸矩形 → **整条线被裁没**，拖动方向变化 ⇒ "时有时无"。
  - 修复（2026-09-21 14:38，已执行）：删除 `pathMask` 字段与 Show 内 mask 懒创建块；段位置改为回退对齐 `float x = Mathf.Min(i*step, Mathf.Max(0f, distance - segmentWidth));`（末段末端正好停在 distance，图案不压扁）。**通用教训：Unity 里旋转的 RectTransform 上不要挂 RectMask2D 做裁剪，改用几何对齐。**隐藏条件：①指针在格子缝隙/非格子区（`TryGetPositionAt` miss，:2601 RectangleContainsScreenPoint 全 miss）；②target==起点格；③Locked/占用不可合成格；④边界附近 `DragTargetSwitchHysteresisPixels=12`（:34/:2656）粘滞造成闪烁。次要：`SegmentHeight=10`×Game 0.43 缩放≈4.3px 太细易看漏。可选改法：加粗 SegmentHeight / 目标非法时 alpha 降为 0.3 灰显（约 10 行）/ miss 时回退最近格（不建议）。
- **攻击范围预览（Range Preview）实现索引**：全在 `GreyboxBoardView.cs` —— `[SerializeField] Image rangePreview`(:44)、`unitRangeCells`/`unitShowsRange`(:69/:71)、`SetUnitPresentation`(:865)、`ShowRange`(:1439，`sizeDelta = cellSize*radiusCells*2`，圆形尺寸)、`SetRangePreviewVisible`(:1462)。点击链：`DraggableUnitView.OnPointerClick`(:904)→`SelectUnit`(:1320)，两支：`TrySelectPairRange`(:1385 英雄 pair，圆心=两组件格中点) / 基础单位(:1359-1381，Battle 或 Bench，非基础单位在 Bench 不显示，见 :1528-1529)。部署拖拽预览 `ShowDeploymentRangePreview`(:2401)，由 :960 调用，圆心=目标格。半径源：基础单位 `UnitRangeRules.GetRadiusForConfig`（Axe1.5/Rider2/Spear2.5/Bow3.5）；英雄 `HeroCombatState.RangeCells`(:258)=definition+符文 AttackRangeFlat。节点：`ART_LocalRangePreview`/`ART_AiRangePreview`/`ART_PlayerRangePreview` + 子 `ART_RangeOutline`；构建器 `Editor/DragonBoundPortraitUiBuilder.cs:1068 CreateRangePreview`。
- **【实锤：范围圈显示成方形】** 期望 fill=内置 Knob(10905,圆)，实际 V1/V2 场景的 `ART_PlayerRangePreview`/`ART_AiRangePreview` Image sprite = **10913 内置 UISprite（圆角方块）**（普通场景对象，非 prefab 实例）；`ART_RangeOutline` 引用 guid `a8c4be03…` 悬空。`UpgradeRangePreviewsOnly`(:130) 只处理两个 V1 prefab 且从未跑过（prefab 里也还是 10913）。
- **补 RangeOutlineThin.png 不会让方块变圆**（15:13 实测）：①方块是 **fill 层自己**（10913 UISprite）画的，与 outline 无关；②新图 guid = `96dd4092…`(V1) / `e6a26e64…`(V2)，场景 outline 仍引用悬空 `a8c4be03…` —— **场景序列化存 guid，补同名文件不会自动接上引用**，必须重新赋值并保存场景。V1/V2 现均有 `Content/UI/Art/Range/{RangeOutline.png, RangeOutlineThin.png}`。`UiVariantProjectPaths` 只有 V1Scene/V1Ui，无 V2 版。修复 = Editor 脚本遍历两场景 4 处 fill 赋圆图 + 4 处 outline 赋对应变体 png + 保存（Knob 只能编辑期赋值，运行时拿不到）。
  - **已执行（15:15）**：`UiVariantProjectPaths.cs` 补 `V2Scene/V2Ui/V2Resource`；`Editor/DragonBoundPortraitUiBuilder.cs` 把 `PrepareRangeOutlineSprite()` 拆出带路径的重载，并新增菜单 **`DragonBound/UI/Upgrade Range Previews In Scenes`**（:152-231）——对 V1/V2 Greybox_Main 打开场景，遍历名为 `ART_*RangePreview` 的节点，fill.sprite = 内置 Knob(`UI/Skin/Knob.psd`)、color=RangeFillColor、preserveAspect；子节点 `ART_RangeOutline` 赋对应变体的 RangeOutlineThin.png、RangeOutlineColor；保存场景。需用户在 Unity 里执行该菜单（会打开/切换场景，先保存当前未保存改动）。