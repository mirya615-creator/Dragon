# 2026-08-31

- Boss 蔚蓝色 tint 移除方案：改 `EnemyView.cs` `Bind()` L117-122（删 Boss 分支）+ L21-23（删 BossTestColor/SubBossTestColor 常量，StormShield 保留）。注意：EnemyCard 根 body Image 作者色为白色 alpha=0，tint 移除后 Boss 主体不可见、只剩 BossW 动画（与普通敌人一致）；若需可见底图需在预制体把 alpha 设 255 或配 sprite。EditMode 测试只断言动画绑定，不断言颜色，无测试需改。
- EnemyCard 预制体结构核实：`EnemyCard`（根，EnemyView 组件，body Image 作者色 white/a=0）→ `ART_EnemyHpTrack`（血条底，Image 当前用内置 Knob.psd guid 7f82cf1e，尺寸 76×14）→ `ART_EnemyHpFill`（fill，运行时 fillAmount 动画）+ `overHp` 子节点。EnemyView 仅运行时改 fill，track 的 authored sprite 保留。`ART_EnemyAnimation/Image` 挂 Animator，Boss 按波次 6/12/16/20 加载 BossW06~W20。
- `BossHp.png`（Assets/Resources/GameUI/，guid f53090402f141d54584f27797a9c0629，Sprite Single，已就位）。应用于 ART_EnemyHpTrack 方案 A：预制体直接改 Source Image（影响全敌人，因 EnemyCard 共用）；方案 B：EnemyView 加 `hpTrack` 字段 + 在 Bind 对 Boss archetype `Resources.Load<Sprite>("GameUI/BossHp")` 覆盖（仅 Boss）。
- 血条延迟过渡效果实现：EnemyCard 预制体已有 overHp 子节点（ART_EnemyHpTrack 的第一子，渲染在 fill 下方），当前 m_Type:0 Simple，运行时需代码配 Filled。实现要点：(1) EnemyView 加 overHp Image 字段 + displayedOverHpRatio/overHpDelayTimer 状态；(2) ResolveHealthBarView 解析 overHp 并配 Filled/Horizontal/Left；(3) Bind 初始化 overHp=healthRatio，掉血时设 overHpDelayTimer；(4) UpdateHealthBar 拆分——fill 用现有快速 lerp，overHp 用延迟+慢速 lerp 追向 targetHealthRatio；(5) 回血时 overHp 直接 snap 到 target（不滞后）。关键陷阱：原 UpdateHealthBar 的 early return（fill≈target 时 return）会跳过 overHp 更新，必须重构为先跑 fill 再无条件调 UpdateOverHp。
