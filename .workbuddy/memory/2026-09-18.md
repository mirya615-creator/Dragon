# 2026-09-18

## V2 Play 三条报错分析（09:18，未执行修改）
- 报错：`UI asset is not registered. Variant=V2, Key=prefabs/ItemBg|Item` + MainMerchantController.Awake(:167) 总校验失败。
- 根因：**注册表被外部修改、Unity 未重新导入**。证据链：① 磁盘 UiAssetRegistryV2.asset mtime 09:15，含 117 键（42→117），ItemBg/Item 的键+guid+fileID 全部有效（逐个核对过 prefab 锚点）；② 当前 Editor 会话 09:10:26 启动（Editor.log Licensing 时间戳），全程无 "Generated V2 UI registry" 日志 → 09:15 的写入不是本会话菜单所为（外部写入）；③ 本会话三次 Asset Refresh 全部 `changed=0` → 改动从未进 AssetDatabase；④ Play 用的是旧导入数据 → Load 返回 null。
- 错误 3 是级联：Awake 总校验把 offerItemPrefab/ownedItemPrefab==null（即错误1/2）与场景结构放在一起判，任一失败即 LogError+enabled=false。V2 场景商人结构已核对齐全（MerchantPanel/ChantBtn/LotteryContainer/8 个 LotteryItem prefab 实例(缺 (4) 命名但不缺数量)/ItemContainer/ActiveColumn/PassiveColumn/CancleItemPanel/CancleBtn/ConfirmBtn）→ 修好 1/2 后自动消失。
- 修复：回 Unity Ctrl+R（或对注册表 Reimport），最干净是跑菜单 `Regenerate V2 Asset Registry`，成功标志 = Console 出现 `Generated V2 UI registry with 117 keys`，再 Play。
- 顺带发现：`prefabs/Item 1.prefab` 是复制粘贴产物（多余键 prefabs/Item 1，建议删）；注册表第一键 `UIResources/Main/Profile/0` 说明头像仍在 UIResources/Main/Profile（Profile 迁移未做）。
- 教训：编辑器外改 .asset/.prefab/.unity 后必须回 Unity Refresh 再 Play。
## V2 Play 三条报错分析（仅分析未修改）
- 根因：共享代码请求 V1 风格键 `ComponentUI/shovel`、`GameUI/BeachSelect`、`GameUI/BoardSelect`（GreyboxBoardView.cs 常量），V2 文件实际在 `UIResources/Component/shovel.png`、`UIResources/Game/game/BeachSelect|BoardSelect.png`，重新生成的 V2 注册表键对不上 → Load null → EnsureSelectionSprites 抛 InvalidOperationException → Bootstrap 初始化失败 → LoadingPanel 拉住遮罩。
- 注意：UiAssets 兜底 Resources.Load 仅在「无激活注册表」时生效，V2 已激活则缺键只 LogError 返回 null。
- `prefabs/BoardSelect` 键在 V2 一致所以没报。修复建议：V2 按代码键路径摆放三张图 + Regenerate V2 Registry；另注意 V2 BoardSelect.prefab 可能跨变体引用 V1 图 guid。
## Login 进度条改 Fill Amount 方案（仅输出未修改）
- 现状：FillImg 的 Image m_Type=0(Simple)，fillAmount 无效；进度靠 `anchorMax.x = progress` 拉伸。两套驱动：LoginController.PrepareStartupLoading(143-149)/SetStartupLoadingProgress(238-243) 与 SceneLoader.ConfigureProgressFill(220-234)/SetProgress(172-181)，操作同一 FillImg。
- 方案：场景里 FillImg 改 Filled+Horizontal+Left（V1/V2 Login.unity 约 1392-1398 行），Rect 保持美术原样；两处代码删掉 anchorMax/anchorMin/sizeDelta 覆写，只 set fillAmount。
- 场景实测：LoadingImg V1 1000x128 / V2 1007x90（均被 LoginSafeAreaLayoutAdapter 横向拉成屏宽-80）；FillImg V1 全拉伸，V2 877x45 居中（不跟随安全区，可选改相对锚点 0.0645/0.9355）。
- 风险：圆角底框需 Mask；Sliced 精灵不适合 Filled；startupLoadingFillRect 字段在 ValidateHasRequiredUi(111) 仍有引用，删字段需同步改。
## 签到第7天 Image (6) 不再由代码换图（仅输出方案未修改）
- 节点 `Canvas/SafeAreaRoot/MainPanel/SignPanel/Image/Image (6)` = 第7天卡，V1/V2 Main.unity 同 fileID 798608792（V2 另有同名节点在 MerchantPanel/Bg/MyItemBg/ItemContainer/PassiveColumn 下，不冲突）。
- 改动源只有 MainSignInController.cs：ApplyDaySprites(288-295) 换 sprite；ResolveView(129-165) 运行时 AddComponent<CanvasGroup> 并改 Button disabledColor；SetDayState(305-327) 用 CanvasGroup alpha*0.55 变暗。
- 方案：加 PreserveAuthoredSpriteMask = 1<<6，ApplyDaySprites 跳过 index 6；可选连 alpha 也跳过（但 interactable 必须保留）。高亮需求改走子节点 SetActive（方案B）。
- 红线：不能删 Image 组件（ResolveView complete 判定 + Editor/SignInFeatureValidation.cs:72 校验要求 Image+Button）；不能改名/改层级；Today/other 资源第1-6天仍用（校验 73-78 行）。
## 商人页 ItemBg 运行后尺寸被改（仅分析未修改）
- 根因：ChatItemCon 上挂 VerticalLayoutGroup（guid 59f8146938fff824cb5fd77236b75775）。V1：容器 817.57x671.83、ChildControlWidth=1 → 子项宽度被强制 900→817.57；V2：容器 830x695.5、Control W/H=0 但 ForceExpand=1、ChildScale=1、Alignment=4 → 布局重建用 SetInsetAndSizeFromParentEdge 重写 anchors/sizeDelta/anchoredPosition。
- prefabs/ItemBg.prefab 根：900x214、锚点(0,0)-(0,0)、pivot(0.5,0.5)、无 LayoutElement（V1/V2 一致）。
- MainMerchantController.Populate(745-754)：先 Destroy 容器内所有子节点，再按 offer.Products 数量 Instantiate → 场景里手编的 ItemBg 运行时已被销毁，改它无效，必须改 prefab。
- StretchToSlot(1079-1088) 只作用于 ItemContainer 的 Item，不影响 ChatItemCon/ItemBg。
- 修法方向：A 改 prefab 尺寸 + 容器宽度≥900；B 关 V1 ChildControlWidth / 两边 ForceExpand；C 加 LayoutElement preferred 900x214。
## V2 商人 ItemBg「运行时 = 手动编辑值」方案（仅输出未修改）
- V2 场景 ChatItemCon 下 3 个 ItemBg 是 prefab 实例（源 guid e4d1bb07bf5cbf541aedbe61df02edc8 = V2/Content/Resources/prefabs/ItemBg.prefab，V1 的是 d4a6d897c0deca145b608192abaf8ddb，互不影响）。手动 override：ItemBg 793x195 @(415,-114.25)、ItemBg(1) 793x200 @(415,-345.25)、ItemBg(2) 793x200 @(415,-578.75)，anchors (0,1)-(0,1)、pivot 0.5/0.5。
- 容器 ChatItemCon：830x695.5，VerticalLayoutGroup（guid 59f8146938fff824cb5fd77236b75775）参数：Padding 0、Alignment 4(MiddleCenter)、Spacing 0、ForceExpand W/H=1/1、Control W/H=0/0、ChildScale W/H=1/1。Control 关闭 → 子项自身尺寸被保留，只驱动 anchors/anchoredPosition；ForceExpand 把剩余空间按 surplus/count 当成项间间隙（手动值下=33.5），再按 MiddleCenter 居中（上下各 16.75）。
- 运行时差异根因：Populate(745-754) Destroy 全部子节点后 Instantiate prefab → 新实例取 prefab 的 900x214（V2 prefab 根 rect：sizeDelta 900x214、锚点(0,0)-(0,0)、无 LayoutElement）→ 于是高 214 vs 195/200、宽 900 > 容器 830 溢出、间隙 (695.5-642)/3=17.83 vs 33.5。
- 关键数据：ItemBg 根 Image 精灵 = UIResources/Main/Merchant/矩形 4.png，像素 793x195（spritePixelsToUnits 100）→ preferredSize 793x195，故手动宽 793 恰等于精灵宽。
- 修法（V2 only，不动任何 .cs）：① 改 V2 ItemBg.prefab 根 sizeDelta 900x214 → 793x195（或 200）；② 可选精确对齐：ChatItemCon VLG 关 ForceExpandHeight、Spacing=33.5、Padding.Top=16.75、Alignment=UpperCenter；③ 场景内 3 个实例的 override 同步/Revert 以便编辑态预览一致；④ 保存 + Refresh。3 个商品固定（LocalMerchantGateway.ProductsPerOffer=3）。
- 不能做到：首张 195 其余 200（需代码按 index 区分，不建议）。
## V2 生命「蛋」UI 换图（仅输出方案未修改）
- 生命 UI 真身：`Greybox_Main` 里 `Canvas/SafeAreaRoot/DragonBoundPortraitScreen/ART_ScreenBackground/ART_FixedBoardCanvas/ART_FixedBoardTerrainLayer/{ART_PlayerGoal_7_0,ART_AiGoal_0_9}/HpBg`，HpBg 下 3 个 `heart1 / heart1 (1) / heart1 (2)`（名字沿用 heart，实为生命图标）。
- 驱动代码 `DragonBound/Runtime/Presentation/DragonBoundScreenView.cs`：`BindGoalHealthViews`(423-455) 按名字找 ART_PlayerGoal_7_0 / ART_AiGoal_0_9；`GoalHealthView.Initialize`(489-537) 收集 `name.StartsWith("heart")` 的子节点、GridLayoutGroup 改 FixedColumnCount=3/LowerLeft、以 hearts[0] 为模板；`EnsureCapacity`(583-591) 血量>3 时 Instantiate 克隆（继承同一 sprite）；`RefreshLayoutSize`(562-581) 按 cellSize.y 重设 HpBg 高度（3 血=44.95，>3 血=80）。
- V2 已生效（14:17 保存）：AI 三个 heart sprite guid = f47adb5367fc0424aaab2b42bfd6aa51（965_101.png，186x228）；Player 三个 = 3365f44c0639ab845bcd8fd7c7960725（962_101.png，186x225）。V1 场景仍是 6f4d859b(玩家)/80e4a3a6(AI)，未受影响。
- 版式：HpBg 100x44.95，GridLayoutGroup cellSize 20x40、spacing.x 10、padding.left 10；heart Image m_Type=0、PreserveAspect=1 → 蛋实际只画 20x24.5（占格高 61%）。cellSize.x 上限 (100-10-20)/3≈23.3；spacing 改 5 可到 26.7。
- 红线：heart 节点名不能改（StartsWith("heart")，改名 → Initialize 抛 InvalidOperationException 整屏失败）；HpBg 必须有 GridLayoutGroup。两图在 V2/Content/Resources/UIResources/Game/game/ 下，Regenerate V2 Registry 会生成 key `UIResources/Game/game/微信图片_…`（当前是场景直引，不依赖注册表）。
- V2 场景仅 1 处 prefab 实例（BeachItem，V2 自己的），goal/heart 都是场景内普通节点，无跨变体引用。
## 拖拽连线 SlectRoad 拉伸 → 叠加方案（仅输出未修改）
- 拉伸点：`Assets/DragonBound/Runtime/Presentation/DragArrowPreviewView.cs` Show() 第 56 行 `rect.sizeDelta = new Vector2(delta.magnitude, DragPathRectHeight=180f)`，把方形贴图拉成长条；shaft=ART_DragArrowShaft（stretch 全 root），root=ART_DragArrow（pivot 改 (0,0.5)、旋转 angle）。
- 调用方：GreyboxBoardView.UpdateDraggedUnit(954) 与 UpdateShovelDragPreview(2365)，父节点 fixedBoardCanvas.EnsureDeploymentGuideLayer()。选中框：BeachSourceSelect（源）/ BeachSelect（目标），中间就是这条线。
- 资源 key `GameUI/SlectRoad`：V1=Content/Resources/GameUI/SlectRoad.png（1254x1254，guid 7628c5d1…），V2=Content/Resources/UIResources/Game/game/SlectRoad.png（100x100，guid 1f04c0cf…）；两侧注册表 key 一致（V2 是手工对齐，regenerate 会变成 UIResources/… 需再改回）。两边 spriteMeshType=1(FullRect)、ppu=100 → 可直接 Tiled。
- 场景结构（V1/V2 各 2 套，AI/Player）：ART_DragArrow(120x10, inactive) → ART_DragArrowShaft(stretch, Image) + ART_DragArrowHead(anchor .86-1, Text ">"，代码里 SetActive(false))。
- 项目内已有"叠加"范式：CombatFxView.cs 2269-2288 AbyssalHarpoon Chain（Image.Type.Tiled + pixelsPerUnitMultiplier = sprite.rect.height / chainHeight，hook 独立节点 anchoredPosition.x=长度 不拉伸）。
- 推荐改造：shaft.type=Image.Type.Tiled + pixelsPerUnitMultiplier=sprite.rect.height/(ppu*期望线宽H)，rect 高改 H；或用段池（每段固定 segmentLength，位置 clamp 到 [L/2, dist-L/2] 形成末端重叠）。
- 注意：V1 图 1254px 含大透明 padding → 平铺会稀疏/断裂，需美术裁 padding；线宽守恒公式：原线宽=180*线占比，新线宽=H*线占比。
- 测试依赖：BootstrapPlayModeTests 268/273/299/305/318 断言 IsDragArrowVisible / DragPathSprite 非空 → PathSprite 必须继续返回 SlectRoad。
## 拖拽连线 Tiled 后"线没了"根因（仅分析未修改）
- 用户已按方案 A 把 DragArrowPreviewView 改成 Tiled（TiledPathHeight=24），线消失。
- 像素级分析：SlectRoad 是细线居中+大片透明：V1 1254x1254 线条仅 y599-647(x213-1039)=49px 高占 3.8%；V2 100x100 线条 y45-53(x1-98)=9px 占 9%。
- Simple 时代 180 高矩形把细线放大到 ~7(V1)/16(V2) 单位厚；Tiled 后整张方图=一块 24x24 瓷砖，可见线仅 0.9~2.2 单位，0.44 缩放下 <1px → 不可见。Tiled 数学本身没错。
- 修复方案 A（推荐）：ApplyAuthoredPathSprite 里用 Sprite.Create 裁出线条包围盒做平铺源（按 textureRect.width>500 区分 V1/V2 裁剪常量），multiplier=crop.height/(ppu*TiledPathHeight)，TiledPathHeight 建议 10（Show 的 sizeDelta 高同步）；方案 B：裁剪两张 png（文件名不变，key 不动）。
- PathSprite 返回 Sprite.Create 结果仍非空，BootstrapPlayModeTests 断言不受影响。
## 方案A（Sprite.Create 裁剪平铺）具体修改步骤（仅输出未修改）
- 目标文件唯一：Assets/DragonBound/Runtime/Presentation/DragArrowPreviewView.cs。
- 裁剪常量（自纹理底部起算 y）：V1(1254px) x=213 y=607 w=827 h=49；V2(100px) x=1 y=47 w=98 h=9；判据 pathSprite.texture.height>500。
- 三处改动：删 DragPathRectHeight(10行)、TiledPathHeight 180→24→10、Show(57) sizeDelta 用 TiledPathHeight、ApplyAuthoredPathSprite(78-101) 整段重写加 Sprite.Create 缓存 tiledPathSprite + multiplier=crop.height/(ppu*TiledPathHeight)。
- 不改场景（shaft Image m_FillCenter=1、m_RaycastTarget=0 已适配 Tiled）、不动注册表、Sprite.Create 无需 Read/Write。
- 调参：瓷砖宽 = H*cropW/cropH（V2≈109、V1≈169 单位/块，格子≈116）；图案太稀→调小 TiledPathHeight(6-8)；线厚=TiledPathHeight。
## V1/V2 拖拽线复测异常根因（已实锤，未修改）
- V2 黄条根因：UiAssetRegistryV2.asset 09:15 重新生成后丢了手工对齐的 GameUI/SlectRoad key（只剩 UIResources/Game/game/SlectRoad）。UiAssets.Load 缺 key（激活注册表时）LogError+null（UiAssetRegistry.cs:186-192，编辑器也无兜底）→ ApplyAuthoredPathSprite 在赋 sprite 前 throw → shaft.sprite=null → uGUI Image 无 sprite 渲染默认白块 × tint(0.96,0.84,0.28)=(245,214,71)，截图取色 (245,214,85) 实锤。V2 的"拉伸黄条"从最初就是无贴图 bug，平铺代码在 V2 从未执行。
- V1 看不见根因：key/裁剪均正确（8 颗金菱形 alpha255 平均色(248,204,12)），但 49px@ppu100 天然 0.49 单位，TiledPathHeight=10 → 钻石仅 ~2.6 屏幕像素(0.44缩放)且米色棋盘低对比 → 不可见。Simple 时代可见是横向拉伸放大所致。
- 修复：① V2 恢复 key（复制 png 到 V2/Content/Resources/GameUI/ 或手工加条目 + Regenerate）；② TiledPathHeight 10→24 或按变体（字段 currentPathHeight：V1=24/V2=12，Show 用字段）；③ 可选 V2 两处 shaft Image Color 改白（V2 图案是 teal(60,146,175)，黄 tint 染绿）。
- 教训：先查 Console 是否有 "UI asset is not registered"；Registry regenerate 会覆盖手工 key。
