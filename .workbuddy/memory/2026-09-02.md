# 2026-09-02

- 随机流协议与 Stage 配置分析，输出至 `Docs/随机流协议与Stage配置分析.md`。关键事实：
  - `recruit_random` / `wave_rng` / 已发布 Stage 数据不在 Unity 仓库——属 Go 后端（工作区无该工程）。客户端等价物：种子派生分流方案。
  - RNG 本体 `GameShared/Runtime/Random/RunRandom.cs`：PCG（LCG 乘数 6364136223846793005/增量 1442695040888963407，XSH-RR 32 位输出），播种固定消耗 2 次原始输出，NextUnit 取高 24 位，NextInt 拒绝采样。
  - run_seed：十进制 uint64 串，`(u64 ^ (u64>>32))` 折叠为 int32；run_nonce：Base64URL 16 字节。子流：player.recruit / ai.recruit / combat / ai.decision。
  - ⚠️ 线上/离线子流派生不一致：GoUnary 网关 `FNV(stream)^seed`（无末乘）vs Local 网关 `(FNV(stream)^seed)*16777619`——同一 runSeed 两网关结果分叉。
  - `critical_random_sequence` 客户端恒空串（GoUnaryGameplayRunGateway L168）；战斗运行时无暴击系统。
  - 无黄金随机测试向量；唯一随机性测试是 RunRandomTests 种子 73 自洽性（无期望数值断言）。诊断种子通道 UseDiagnosticSeed → RunId `diagnostic.<seed>`。
  - Stage：客户端仅 `stage-001`（ClientServiceConfig.asset），内容服务器权威（stage_snapshot_digest）。本地 greybox 配置 `TwentyWavePressureConfiguration.CreateCoreLoopV2`：20 波、HP 曲线 R1 25.5→660、Boss 波 6/12/16/20。
  - 奖励：金币/碎片服务器结算（settlement.total_gold）；符文掉落客户端权威（RuneDropRules：资格 W≥3，掉率 12/18/28/40%，每局上限 4 次，史诗 25% 整卡）。
- 用户确认上述 7 项为 finish 接口（POST /v1/runs/{id}/finish，GoRunFinishRequest）所需。客户端 finish 现状（GoUnaryGameplayRunGateway L158-170）：发 ticket_signature/result/reached_wave/remaining_health/final_event_sequence/final_event_hash/critical_random_sequence(空串)/event_summary(仅 run_end 单事件)。行动清单已给出：P0=统一两网关派生算法+协议文档+黄金向量；关键待确认=后端验证模式（服务器重放 vs 客户端上报随机流）。
- 分析 ART_AttackLine(Clone) 生成链路：模板由 `DragonBoundPortraitUiBuilder.cs` L482-488 创建（黄色 120×7，CombatFxAuthoring: StretchBetweenPoints/0.28s/fade），L526 注入 `CombatFxView.attackLineTemplate`（L28）。触发点：`CombatFxView.OnCombat()` L435-437 `AttackKind.Single`（BasicUnitCatalog L61 基础单位 + PressureRaceSideRuntime L918）和 L433 `AttackKind.Item` 的 `itemImpactTemplate ?? attackLineTemplate` 兜底。实例化在 `SpawnImage` L1376 `Instantiate(template, parent)`。停用方案：Single case 改 break、Item 兜底去掉；不删模板（PortraitUiPrefabTests L562 断言模板存在）；PlayMode 测试 L686-690/L745-750 断言 Clone 数==0，移除后不受影响。
