# 2026-09-17

## V2 小怪 Spine 走路动画接入（方案，未执行）
- 新增资源：`V2/Content/Resources/Animations/Enemy/01/AxeMinion/Spine/`（atlas.txt/json/png + 导入生成的 _Atlas/_Material/_SkeletonData，scale 0.01，动画名 `Walk`，skin default，Spine 3.8）。运行时 `Assets/Spine` 是 spine-unity **3.8**，与 json 版本匹配；`SkeletonGraphic.cs` 存在。
- 接入点：`V2/Content/UI/Prefabs/Components/EnemyCard.prefab`（guid 7a592719359185a4a93c1b0e060257ad），V2 Greybox_Main 场景已引用该 V2 预制体；V1 场景/DragonBoundPortraitScreen/Battlefield 引用 V1 guid → 改 V2 预制体不碰 V1。
- EnemyView 动画路径：`ART_EnemyAnimation/Image`(+Animator)，控制器 `UiAssets.Load<RuntimeAnimatorController>("Animation/…")`；**V2 注册表无 Animation/* 键** → controller 为 null → 动画层当前被 `enabled=false`，正好让位给 Spine。
- 坑：`ResolveWaveAnimationView` 用 `FindUi("Image")` 找节点，Spine 节点**不能叫 Image**；删掉 Image 子节点会让方法提前 return，`ART_EnemyHpTrack.SetAsLastSibling()` 不执行 → 血条不再置顶。
- 方案 A（零代码）：ART_EnemyAnimation 下加 `ART_EnemySpine` 节点挂 SkeletonGraphic(Walk, loop)，保留 Image/Animator 节点。
- 方案 B（长期）：EnemyView 加 SkeletonGraphic 分支；需在 `DragonBound.Runtime.asmdef` references 加 `"spine-unity"`（asmdef 名小写带连字符）；V1 无该组件则走原路径。
- 后续：目录约定 `Animations/Enemy/{序号}/{Archetype}/Spine/`；放 Resources 下需 Regenerate V2 Registry 才会进键表。

## V2 EnemyCard Spine 接入定稿（16:02，用户方案：Image 改空物体挂 SkeletonGraphic）
- 用户层级：ART_EnemyAnimation/Image = 空物体（移除 Image 组件）+ SkeletonGraphic(Walk loop)，Scale 已调好。不新增兄弟节点。
- 代码验证（EnemyView.cs）：ResolveWaveAnimationView:1066 FindUi("Image") 仍命中节点→GetComponent<Image>()=null→1069 early return。连锁：① Animator 永不添加，Sprite 动画链路自然关闭；② GetWaveAnimationController 不再执行 → `Animation/*` 缺键红错消失；③ 血条 SetAsLastSibling(1058) 在 return 之前 ✅；④ PlayHitShake/SetWaveAnimationMirrored 作用于 ART_EnemyAnimation 根，Spine 跟随 ✅。
- 两个注意点：VisualImpactPosition:128 回退到卡片根位置（受击特效落点需验证）；不要改 ART_EnemyAnimation 根的 localScale（会被捕获为 authoredWaveAnimationScale 参与镜像翻转）。
- DragonBoundPortraitUiBuilder.cs:27 只写 V1 路径（V1Ui），V2 prefab 无重建覆盖风险。
- 注册表：SkeletonGraphic 直接 guid 引用 SkeletonDataAsset，不走 UiAssets.Load → 运行时不依赖 Regenerate；跑了也无害。
- 节点仍叫"Image"但已无 Image 组件：故意保留（改名会触发 animationRoot.GetComponent<Image>() 回退，可能绑到根节点 Image 上加 Animator）。

## V2 四种小兵按波次循环（16:17，方案未执行）
- 资源：`Animations/Enemy/{01..04}/{AxeMinion,SpearMinion,SwordMinion,BowMinion}/Spine/*_SkeletonData.asset`，全部 spine 3.8、scale 0.01、动画名统一 `Walk`、skin default。图集均为 **4096×2048**（约 2.3-2.5MB/张，4 张 ≈ 9.5MB 源图；RGBA32 入内存 32MB/张 → 必须降 Max Size）。
- 波次规则**代码里已存在**：`EnemyView.cs:12 WaveAnimationCount=4`；`BindWaveAnimation` animationIndex=`(Max(1,SpawnWaveIndex)-1)%4`（Boss 用 `-wave` 走 BossW06/12/16/20）。即 W1→01、W2→02…W5→01 循环，与用户描述一致 → Spine 直接复用该 index。
- 推荐：单 SkeletonGraphic 节点 + `[SerializeField] SkeletonDataAsset[] waveSkeletonData`（V2 prefab 填 4 个，V1 空）；BindWaveAnimation 开头分支：index 换 `skeletonDataAsset` + `Initialize(true)` + `SetAnimation(0,"Walk",true)`，并禁用 sprite/Animator。V1 prefab 无组件 → 走原路径。
- 必要改动：`DragonBound.Runtime.asmdef` references 加 `"spine-unity"`（Assets/Spine/Runtime/spine-unity.asmdef name=spine-unity，3.8，有 `Initialize(bool)`/`MatchRectTransformWithBounds()`/`IsValid`，Awake 会自动 Initialize(false)）。
- 不改 asmdef 的备选：V2-only 组件放 Assembly-CSharp（可同时看到 asmdef 与 Spine），EnemyView 只需加一行 `public int BoundWaveIndex {get; private set;}`。
- 坑：Resources 无跨版本排除机制（版本工具只做 MoveAsset 迁移）→ V1 包体也会带这 4 张图集；SkeletonGraphic 每实例独立骨架+mesh，同屏几十个需实测帧率。
- 16:27 用户手改 asmdef 漏逗号（"Unity.TextMeshPro" 后）→ JSON parse error，已修复并 json 校验通过。
- 16:39 现象"任何波次都是斧兵"根因：**V2 EnemyCard.prefab 未保存 A2 新增字段**（YAML 到 `hpTrack: {fileID: 0}` 结束，无 waveSkeleton/waveSkeletonData）→ 运行时反序列化为 null → 1102 分支跳过 → 一直播编辑器预填的 AxeMinion(guid a26b55bd)。教训：代码加 [SerializeField] 后必须**重新保存 prefab** 才会写入 YAML；Inspector 显示了 ≠ 已落盘。
- 已排除：分支位置正确（BindWaveAnimation 首句）、SpawnWaveIndex 来源正常（EnemyRuntime.cs:51，测试断言有 ==4 的敌人）、四个骨架 guid 互不相同。
- 次要点：waveSkeletonData 长度 1 时 `%1=0` 也会"永远第一种"；Boss 分支在 isBoss 判断之前，Boss 也会按 (wave-1)%4 显示小兵骨架。

## V2 Boss Spine 接入方案（16:52，未执行）
- 资源：`Animations/Boss/{W6/Boss1, W12/Boss2Storm, W16/Boss3Blood, W20/Boss4Dragon}/Spine/*_SkeletonData.asset`，全部 3.8、动画名 `Walk`、skin default；图集 **4096×4096**（RGBA32 64MB/张，4 张 256MB）→ 必须降 Max Size 1024。
- 与代码对齐：`GetBossAnimationController`（EnemyView.cs:1142）switch case 6/12/16/20 → BossW06/12/16/20，与目录 W6/W12/W16/W20 一一对应；boss 是**按波次精确匹配**，小兵是 `(wave-1)%4` 循环，两者逻辑不同。
- 推荐：复用同一个 SkeletonGraphic 节点；新增 `[Serializable] struct EnemySkeletonWaveEntry { int wave; SkeletonDataAsset skeleton; float scale; }` + `[SerializeField] EnemySkeletonWaveEntry[] bossSkeletons`；把现有 minion 分支抽成 `ResolveSkeletonForWave(enemy)`（Boss→查表，Minion→(wave-1)%len），去重 key：boss 用 `-wave`、minion 用 index。未命中 boss 波次 → 静态 HashSet 记一次 LogWarning 后走原路径。
- 尺寸：SkeletonGraphic 3.8 **无 initialScale 字段** → 缩放只能改节点 RectTransform.localScale；`RestoreAuthoredTransform`(1263) 只改卡片根 RectTransform、`SetWaveAnimationMirrored` 只翻 ART_EnemyAnimation 根 → 改子节点 localScale 安全，不冲突。
- 不推荐：4 个 boss 各挂独立 SkeletonGraphic 节点（图集常驻内存）。
- 直接 prefab 引用，不走 UiAssets.Load → 无需 Regenerate；死亡动画只有 Walk，Boss 死亡仍用现有 VFX。

## 方案A 具体步骤核对（17:20）
- 现状核对（EnemyView.cs）：`using Spine.Unity` ✅；`waveSkeleton`(76)/`waveSkeletonData`(77)/`EnemySkeletonWaveEntry` 结构体(79-85)/`bossSkeletons`(87)/`boundWaveSkeletonIndex`(89) 已存在；`BindWaveAnimation`(1111) 骨架分支已贴但**引用了未定义的 `ResolveSkeletonForWave`(1115) 与 `authoredSkeletonScale`(1125)** → 当前必编译报错。
- 还缺：① `authoredSkeletonScale`/`skeletonScaleCaptured` 字段（建议插在 109 行后）② 在 `ResolveWaveAnimationView` 的 position 捕获块后（1071 行后）补捕获 ③ `MissingBossSkeletonWaves` HashSet（放 36 行旁）④ `ResolveSkeletonForWave` 方法（放 BindWaveAnimation 之后、1171 GetBossAnimationController 之前）⑤ 1133 行多余缩进。
- 关键：`Awake()`(415) 已调 `ResolveWaveAnimationView()` → `waveAnimationRoot` 与 `healthTrack.SetAsLastSibling()`(1079) 在 Bind 前完成，所以骨架分支里早退是安全的（不影响血条置顶/抖动/镜像）。
- 预制体现状：`waveSkeleton: {fileID: 7132132141704703047}` ✅、`waveSkeletonData` 4 条 guid 正确（01 a26b55bd / 02 a6486797 / 03 10578df2 / 04 d9988196）✅、**`bossSkeletons` 完全缺失**（需补 4 条并 Ctrl+S）。SkeletonGraphic preview 当前 = Boss1(856560b7)。
- 节点基准：Image(SkeletonGraphic) `m_LocalScale {0.035, 0.035, 0}`、`m_LocalRotation (0,1,0,0)` 即 Y 180°、anchoredPosition (2.6,-5.3)、sizeDelta 0。
- Boss 骨架 guid：W6 Boss1=856560b7 / W12 Boss2Storm=8f2b55a3 / W16 Boss3Blood=80f98701 / W20 Boss4Dragon=1b130866。
- 纹理：4 张 Boss 图集均 4096²，meta maxTextureSize 已=**2048**（非默认 4096），mipmap off、isReadable off、Android `overridden:0` 继承默认（compression normal/format auto）。RGBA32 最坏 16MB/张 ×4=64MB；小兵 4 张 1024×512。
