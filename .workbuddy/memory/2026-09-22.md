# 2026-09-22

- 分析「招募出铲子后解锁地图格」实现逻辑（只读，未改代码）：
  - 链路：RecruitDeck 抽卡（ShovelRecruitmentState Preview/Commit 两段式，概率 20/35/50%，3 连保底，资格=场上还有 Locked 格）→ 铲子卡(Kind=Shovel, ITEM_SHOVEL)占一个 Bench 格位 → GreyboxBoardView 点选(1328-1339)/拖拽(892/975) → ShovelUnlockService.TryUnlockCell → BoardGrid.TryUnlock(Locked→Battle, 发 CellUnlocked) → 消耗 bench 铲或 granted 计数。
  - ShovelUnlockService 是唯一解锁入口（`Runtime/Recruitment/ShovelUnlockService.cs`），不做邻接校验，任意己方 Locked 格或 Boss 锁定格均可；外部发放走 GrantShovel / TryAddRewardShovelToFirstEmptyBench。
  - Bootstrap 672-673/715-716 为 Player 与 AI 各装配一套；pity 状态随 RecruitDeck 快照 Capture/Restore 持久化。
  - V1/V2 完全共用此业务链路，唯一变体差异是铲子贴图 `ComponentUI/shovel` 经 UiAssets 走各自注册表。
## V2 顶部填充带与背景接缝断开（2026-09-22 分析，未执行）
- 结构：ART_ScreenBackground 在 SafeAreaRoot 下（固定1080宽居中、纵向拉伸1960源）；ART_SafeAreaTopFill(+Art 子节点 BgLong 1080×400) 直接挂 Canvas 盖刘海区，Art 底对齐带底（=背景顶边接缝）。
- 主因：SafeAreaEdgeFill.ApplyArtSize 把 Art 宽度设为画布宽（本机≈958–978），横向缩放≈0.89；背景已是固定1080=1:1。接缝两侧同一构图宽度差~11%，内容错位。画布宽=1440·√(W/H)，仅 16:9 时=1080 两者才重合。
- 纵向没问题：Art 底边与背景顶边都钉在接缝上，边对边衔接与拉伸无关。
- 修复方向：Art 宽度跟随背景固定1080居中（注意 artRect 现为横向 stretch 锚点，sizeDelta.x=0，需改锚点/sizeDelta 或直接引用背景 RT 宽度）。

## 铲子解锁格子动画方案（2026-09-22 输出，**已执行落地**）
- 文档：`Docs/铲子解锁动画_实现方案.md`（状态已更新为已落地）。触发器用现成的 `ShovelUnlockService.ShovelUsed(GridPosition)`（点选/拖拽/AI 三路统一）。
- 挖掘动画：新组件 `ShovelDigEffectView`，铲子 ghost 挂 heroEffectLayer（仿合成 ghost 模式，避免被 RefreshUnits 销毁），底端 pivot 晃 ±22°×3 + squash，0.55s 手写协程（项目无 DOTween）。
- 擦除过渡：推荐 RectMask2D 双层擦除（零新 shader）；坑：`HandleBoardChanged`(:1969) 目前全量 `RefreshCellStates` 会瞬切贴图，需按 `CellUnlocked` 单格分流并延后 sprite 切换；RectMask2D 不能带旋转（DragArrowPreviewView:113 有先例）。
- 时序：逻辑即时解锁、视觉延迟（挖 0.55s + 擦 0.4s 重叠），不阻断部署。PR 拆分：①擦除 ②挖掘 ③可选悬停抖/粒子/方向跟随。
- **落地记录**：新建 `Runtime/Presentation/ShovelDigEffectView.cs` + `CellUnlockWipeView.cs`；`GreyboxBoardView` BindShovelUnlockService/OnDestroy 订阅退订 ShovelUsed、新增 HandleShovelUsed、HandleBoardChanged 按 CellUnlocked 分流（先 Play 捕获旧贴图再 RefreshCellStates）；`GridCellView` 新增只读属性 CurrentDevelopmentSprite 供擦除捕获。RectMask2D 擦除的坐标推导：mask anchorMin=(p,p)→(1,1) 收缩向右上，lockedCopy 锚 mask 左下角每帧反位 anchoredPosition=(-pW,-pH) 保持贴格子，edge 高亮骑 mask 左下角。编译验证：dotnet build DragonBound.Runtime / Tests.EditMode 均 0 错误（csproj 被 git 忽略，手动加 Compile 项仅本地验证用，Unity 会自动重生成；Assembly-CSharp 因 Firebase.App.Internal 缺 DLL 编不过为既有环境问题，与本次无关）。PR-3（悬停预抖/粒子/方向跟随/AI 开关）未做。
- 已执行修复：SafeAreaEdgeFill 新增 widthReference(RectTransform) + fixedWidth(1080)，ApplyArtSize 宽度改为 优先级 widthReference → fixedWidth → ResolveCanvasWidth()（画布宽仅作兜底），并将 artRect 锚点从横向 stretch 改为居中固定宽（anchorMin/Max 0.5,0；sizeDelta=(width, width/aspect)），侧向溢出与背景一样裁掉。
- V2 场景 Greybox_Main 同步：fill 组件块加 widthReference: {fileID: 99241378082185544}（指向 ART_ScreenBackground RT）+ fixedWidth: 1080；ART_SafeAreaTopFillArt RT 静态值改为 anchor 0.5/0.5、sizeDelta 1080×400（与运行态一致，编辑器预览即可见正确衔接）。仅 V2，V1 无此节点。

## V2 部署时误播“升级烟雾”分析（2026-09-22，未执行）
- 现象：单位/组件拖到地图格落地（回弹动画）瞬间，V2 会播一次轻微烟雾。
- 根因：V2 UnitCard.prefab 的 ART_LevelUpFx 当前 `m_IsActive: 1`（用户后续在 Unity 里重做该节点时被改回激活）。节点带 Animator(controller UnitLevelUp) 且 m_Sprite=0 → 卡片 Instantiate 时 Animator 自动播放默认 state 一次（controller speed 0.2 × clip 0.1167s ≈ 0.58s），正好与落地回弹同帧；播完 LoopTime=0 停在最后一帧（alpha≈67/255）→ 残留淡淡一层。
- 为何代码没兜住：DraggableUnitView.ResolveLevelUpFx 因 levelUpAnimator 已序列化赋值，**第 544-546 行提前 return**，跳过了 fallback 分支里那句 node.SetActive(false) → 从未预隐藏该节点。
- 部署流程本身没有调用 PlayLevelUpFx（事件只由 :732 压力赛调级 / :916 合成触发）。
- 修复方向：prefab 该节点改 m_IsActive:0；把初始 SetActive(false) 提到 early-return 之前（Awake/Resolve 时无条件隐藏），并检查场景内已有 override 实例。
- 后续核查（同日）：`UnitCard.prefab` 的 ART_LevelUpFx 已是 `m_IsActive: 0`（部署误播烟雾的资源侧已修）。UnitLevelUp.anim 实为 7 帧 sprite 换帧 clip（60fps、0.1167s、只动 Image.m_Sprite、无位移缩放曲线），控制器 UnitLevelUp.controller 同目录、仅被 UnitCard.prefab 引用。触发链：unitDestination.BasicUnitLevelChanged(叠加升级)/BasicUnitMerged(合并) → GreyboxBoardView:569 订阅 → HandleBasicUnitLevelChanged/Merged(:1800/:1806) → RefreshUnits + PlayBasicUnitLevelUpFx(:1812) → DraggableUnitView.PlayLevelUpFx(:526)（懒解析 ART_LevelUpFx + Animator；协程播 levelUpFxDuration 后隐藏；duration 从 controller clip 解析，兜底 0.6s、上限 2s）。V1 无该节点，PlayLevelUpFx 无副作用。

## 招募飞入动画方案（2026-09-22 输出步骤，用户手动改，未验证落地结果）
- 现象迭代：初版方案 A（PlayRecruitDropStagger 协程 + 复用 PlayDeploymentFlight）实测出现"5 张先突然出现在 bench，再从按钮飞入"。
- 根因：RefreshUnits 同步建 view 即可见，而协程首张要等 stagger(0.10s) 才 SetDeploymentVisualHidden(true)。
- 方案 C（最终输出步骤）：GreyboxBoardView 新增 `pendingHiddenFlightIds` + `MarkUnitsForHiddenFlight()`（须在 RefreshUnits 之前调）+ RefreshUnitsCore SnapUnit 后 else-if 分支 `pendingHiddenFlightIds.Remove(id)` 时立即隐藏 view；协程去掉多余 `yield return null`；controller 的 onCompleted 里补一次 RefreshUnits 刷 label。落地时搜 `pendingSynthesisPairIds`/`FinishDeploymentAnimation`/`SnapUnit(occupant.UnitId)` 作锚点。

## 部署选中框 BeachSelect/BoardSelect/SlectRoad 实现分析（2026-09-22，只读）
- 全在 `GreyboxBoardView.cs` + `DragArrowPreviewView.cs`，全部运行时生成，经 UiAssets 按变体加载：prefab `prefabs/BoardSelect`、sprite `GameUI/BeachSelect|BoardSelect|SlectRoad`。
- 源选中：Initialize 时 BindBeachSelection 给每个 beach 格建 "Select"（BeachSelect sprite）；非 beach 格走单例 sourceSelectPreview("BeachSourceSelect")。
- 目标选中：单例 boardSelectPreview（注意 GameObject 名字被写成 "BeachSelect"，易混淆）父物体动态 reparent 到目标格 ContentAnchor，BoardSelect sprite。
- 连线：DragArrowPreviewView 把 SlectRoad 裁剪成平铺单元（V1/V2 裁剪常量不同 + 段高 100/10），按距离生成 N 段 Image 链，atan2 旋转；铲子拖拽(Locked 格)复用同一套三个视觉。invalid 目标 HideDragArrow，CompleteDrag 后 ClearBeachDragPreview。

## V2 SlectRoad 虚线间隔加大方案（2026-09-22 输出方案，未执行）
- 实测 V2 贴图 `Variants/V2/Content/Resources/UIResources/Game/game/SlectRoad.png`：**100×100 RGBA，spriteMeshType=1(Tight)**，线条条带 y=45..53（高 9px，与 V2LineH 一致），**6 段虚线，每段 15px，间隔仅 2px**（周期 17px），x 跨度 0..98。Tight 已裁掉两端空白 → 文件里 V2LineX/YBottom/W/H 常量实际走不到（只在 FullRect 时兜底）。
- 当前渲染：tile=整条 99×9 → segmentWidth=99/9*段高；**step=segmentWidth-4(SegmentOverlap)** 会把相邻 tile 的末段与首段吃掉 ~2px → 每个 tile 接缝处两段虚线粘连（节奏不均）。这是"虚线间隔看起来偏小"的第二个成因。
- 方案（推荐，纯代码）：crop 改为**单段虚线** `new Rect(textureRect.x, textureRect.y, 15, textureRect.height)`，新增 V2 专用正间隔常量（源像素，建议 6~10）+ 运行时 `segmentGap`；step=segmentWidth+gap；段数公式改 `max(1, ceil((distance-segmentWidth)/step)+1)`（旧公式 ceil((d+overlap)/step) 在正间隔下会覆盖不足）；V2 不用 SegmentOverlap。**必须给 V1 保留原分支**否则 V1 菱形虚线段数会变。
- 美术方案（备选）：6 段×15 + 间隔 6 → 贴图宽约 120px（同路径替换无需重生成注册表，Tight 自动适配），但接缝粘连仍需代码去掉 overlap。
- 缓存坑：`ApplyAuthoredPathSprite` 里 `tiledPathSprite` 的复用守卫只比 `texture`，改裁剪常量后同 session 内会拿旧平铺单元，需把裁剪尺寸纳入守卫。
- 验证：PlayMode `BootstrapPlayModeTests.cs:271-273` 断言 `DragPathSprite` 与 `UiAssets.Load<Sprite>("GameUI/SlectRoad")` AreSame，但实现里 shaft.sprite 被替换为 Sprite.Create 的平铺单元，该断言可能本就与实际不符，改后跑一遍确认。
- **具体步骤已输出（用户手动改）**：全部在 `DragArrowPreviewView.cs`。①常量区加 `V2DashSourceWidth=15 / V2DashSourceGap=8`；②加运行时字段 `segmentGap`（默认 0=V1 行为）；③ApplyAuthoredPathSprite 把变体 ternary 提为 `isV2Variant`，Tight 分支里 V2 裁成单段 `Rect(textureRect.x, y, min(15, width), height)`（V1 保持整条），用局部 `singleDashTile` 标记；④clamp 之后按 `gap源×段高/裁剪高` 换算 segmentGap（仅 singleDashTile 时>0，V2 FullRect 兜底路径也走 0）；⑤Sprite.Create 缓存守卫加裁剪宽高比较；⑥Show() 里 step/段数分叉：gap>0 时 idealStep=段宽+gap，gapCount=round((distance-段宽)/idealStep) 且 ≥1，step=max(段宽,(distance-段宽)/gapCount) 均匀分布（首段贴起点、末段贴终点），count=gapCount+1；V1 走原公式。行 116 末段回挪在 gap 模式下因 step 均匀化恰好无操作，不改。当前段高 18 → 渲染段宽 30、间隔 16。

## V2 招募区（bench）单位升级动画方案（2026-09-22 输出，未执行）
- 根因：招募区 5 格单位用 `BeachItem.prefab` 渲染（V2 路径 `Variants/V2/Content/Resources/prefabs/BeachItem.prefab`，key `prefabs/BeachItem`），**该 prefab 只有 root(Image)+Image 子节点+Text(TMP 等级)，没有 ART_LevelUpFx 节点/Animator**；`DraggableUnitView`/`CanvasGroup` 是 `GreyboxBoardView.BindBeachBenchCells`(:2330-2340) 运行时 AddComponent 的，所以连 `levelUpAnimator` 序列化字段都是 null。`DraggableUnitView.ResolveLevelUpFx`(:542) 懒查找 `FindUi("ART_LevelUpFx")` 失败 → `PlayLevelUpFx` 静默 no-op。
- 触发链本身已通、无需改 C#：bench→bench 同款同等级合并（`IsMovableCellType` 含 Bench，`CanResolveOccupiedDrop`/`ResolveOccupiedDrop`:874-918 发 `BasicUnitMerged`）→ `GreyboxBoardView:569-570` → `HandleBasicUnitMerged`(:1806)/`HandleBasicUnitLevelChanged`(:1800) → `RefreshUnits()` + `PlayBasicUnitLevelUpFx(merged.TargetUnitId)`(:1812) → `unitViews[id]`（RefreshUnitsCore:767-795 里 bench 用 `beachItemView`，也会入 unitViews）→ `PlayLevelUpFx`。
- 方案 A（推荐，零代码）：给 V2 BeachItem.prefab 加子节点 `ART_LevelUpFx`（名字必须一致，取 `LevelUpFxNodeName`），组件 = RectTransform + CanvasRenderer + Image(raycastTarget 0, sprite 空) + Animator(m_Controller → `Animations/Unit/UnitLevelUp.controller`，guid 4f5485ea83e873f4bb1efb20745dff74)，**m_IsActive: 0**，且放在 root 最后一个子节点（画在 Image/Text 之上）。prefab 内 GUID 引用不需要注册表 key，不用跑 Regenerate。
  - 覆盖范围：场景里 5 个 ImgBg 槽只有第 1 个有 BeachItem prefab 实例（其余 4 个由 `BindBeachBenchCells` 运行时 Instantiate 该 prefab）→ 改 prefab 即全 5 格生效，场景不用动。
  - 几何：BeachContainer 620×120 @anchoredPosition(0,419)（GridLayoutGroup cellSize 120×120 驱动 5 格），卡片本体 100×100；参考 UnitCard 的 fx 是 184×184 @(0,+92)（卡片 124×124）。建议 bench 用 140×140 @(0,+40) 起步（向上溢出约 110，不至于压太多棋盘下沿；屏幕布局上 ART_RecruitButton 在 y=242(尺寸366×139) 即 bench 下方，CampBtn/VoidShovel 在 bench 左右）。
  - 层级：ART_ScreenBackground 子节点顺序 …… ART_FixedBoardCanvas(~y544 起)、ItemContainer、BeachContainer、VoidShovel、ART_RecruitButton、CampBtn → 招募区画在棋盘之上，fx 不会被棋盘盖住；场景无 Mask/RectMask2D，不裁剪。
  - 时长：controller state speed 0.2 × clip 0.1167s = 0.583s，被 `ResolveLevelUpFxDuration`(:600) 夹到下限 0.6s；要改 bench 专属时长就复制一个 controller（别直接改，战场卡片共用）。
  - 风险：`OnDisable`(:1050-1058) 会 StopCoroutine+隐藏 fx，而 RefreshUnitsCore:750-756 每轮把 authoredBeachItemViews 先 SetActive(false) 再按占用激活 → 合并后若再触发一次 RefreshUnits 会打断/掐断 fx（当前 `HandleBasicUnitMerged` 顺序是先 Refresh 再 Play，安全）。fx 挂在卡片根 CanvasGroup 下，会继承卡片 alpha。
- 方案 B（备选，纯代码）：`DraggableUnitView` 增加运行时兜底构造 fx，需要把 controller 注册进 V2 注册表（目前 `Animations/Unit/*` 0 key，需跑 `Regenerate V2 Asset Registry`）再 `UiAssets.Load<RuntimeAnimatorController>`，成本/风险高于 A。V1 不做（V1 无该动画，属设计内 no-op）。

- **方案 A 手动修改步骤已输出（用户手动改）**：全在 `Assets/DragonBound/UI/Variants/V2/Content/Resources/prefabs/BeachItem.prefab`（289 行）。①备份到 `Temp/BeachItem.prefab.bak`；②root RectTransform `m_Children` 列表追加 `- {fileID: 7038472040402347321}`（放最后，Image → Text → ART_LevelUpFx）；③文件末尾追加 5 个新 block（GO id 7038472040402347320、RectTransform 7038472040402347321、CanvasRenderer 7038472040402347322、Image 7038472040402347323、Animator 7038472040402347324）。RectTransform 锚 0.5/0.5、pivot 0.5/0.5、anchoredPos (0,40)、sizeDelta 140×140；GO m_IsActive: 0（关键，否则 Instantiate 时 Animator 自动播）；Image m_Sprite {fileID:0} 空、m_RaycastTarget 0；Animator m_Controller = (9100000, guid:4f5485ea83e873f4bb1efb20745dff74)、m_CullingMode 0 (AlwaysAnimate)。

- **用户已在 Unity 里手动落地（2026-09-22 复核通过）**：`BeachItem.prefab` 现在的实际值 → GO `4332711986383810658` 名 ART_LevelUpFx、`m_IsActive: 0`；RectTransform `8465729890412022266` 锚 **(0.5, 0)**、pivot 0.5/0.5、anchoredPosition **(0, 60)**、sizeDelta **120×120**（注意 anchor 贴卡片下沿，故 y=60 实际是"卡心上方 10px"，卡 100 高；与 UnitCard 同构：anchor(0.5,0)/y=92/卡124 → 卡心上方30）；CanvasRenderer `6477674956628709555`；Image `2280007001464478174`（sprite 空，但 **m_RaycastTarget 仍为 1，建议改 0**）；Animator `6883263927553415975`（controller=UnitLevelUp guid 4f5485ea…、m_CullingMode 0）。root `m_Children` 已含新 RT 且排最后。
  - **代码侧核实（无需改 C#）**：`DraggableUnitView.LevelUpFxNodeName="ART_LevelUpFx"`(:68) 名称吻合；ResolveLevelUpFx(:542) 因 levelUpAnimator 序列化为 null 走兜底 → `transform.FindUi(name)`（`UiNodeBindingMap.FindUi` **未命中只返回 null 不报错**，仅"叶子名重复"才 LogError，:98-101）→ 取 Animator 校验 controller → speed=1/enabled=true/SetActive(false)；时长 = clip 0.1167 / state speed 0.2 / animator speed 1 = 0.583s，被 clamp 到 `LevelUpFxAuthoredSeconds=0.6f`（上限 `LevelUpFxMaxSeconds=2f`）。
  - **注册表无需重生成**：`UiAssetRegistry.Load<GameObject>` 专门挑 `transform.parent == null` 的 prefab 根（:59-66）；`prefabs/BeachItem` 已在 V2 registry(:1395)，根 GO 引用会带全部子节点 → 不用跑 `Regenerate V2 Asset Registry`。
  - **场景无需改**：V2 Greybox_Main 的 BeachItem PrefabInstance（fileID 563704235）`m_RemovedGameObjects/m_AddedGameObjects/m_RemovedComponents/m_AddedComponents` 全空、modifications 无 `m_Children` override → 自动继承；但**编辑态只有第 1 格**有 prefab 实例，其余 4 格靠 `BindBeachBenchCells` 运行时 Instantiate（:2319-2322 用 `UiAssets.Load<GameObject>("prefabs/BeachItem")`），仅 Play 模式可见。
  - 触发器复核：`GreyboxBoardView:569-570` 订阅 → `HandleBasicUnitLevelChanged/Merged`(:1800/:1806) 都是先 `RefreshUnits()` 再 `PlayBasicUnitLevelUpFx`(:1812)；bench 占用格在 `RefreshUnitsCore:767-772` 用 `beachItemView` 并在 :795 入 `unitViews`。已知边界：`RefreshUnitsCore:750-756` 每轮先把所有 beach item SetActive(false) → 0.6s 内再触发 RefreshUnits 会被 `OnDisable`(:1049-1057) 掐断 fx（单次合并安全）。
  - V1 回归安全：V1 `BeachItem.prefab` 无 ART_LevelUpFx（已确认）→ 静默 no-op。

## Bench 区升级动画"始终保持隐藏" 根因排查（2026-09-22，未改代码）
- 现象：用户测试 bench 上合并基础单位后，ART_LevelUpFx 节点一直保持 m_IsActive:0（prefab 默认），动画未播放。
- 排查结论：prefab 与代码逻辑均无 bug，最可能原因是测试方法不在 bench 合并：
  - R1（高）：合并发生在 board 上而非 bench → board view 走 unitPrefab Instantiate（非 BeachItem），无 ART_LevelUpFx 子节点 → DraggableUnitView.ResolveLevelUpFx(:542) 走兜底 transform.FindUi("ART_LevelUpFx") 返回 null → levelUpFxResolved=true → 永远 no-op。
  - R2（高）：用户没真正合并，只抽卡。bench 合并需要 2 个同 ConfigId 同 Level 基础单位 + 拖一个到另一个；BoardRecruitDestination.ResolveOccupiedDrop(:874-918) 在 IsSameBasicUnitAndLevel 才走 BasicUnitMerged?.Invoke。
  - R3（中）：scene prefab instance 未刷新 → Hierarchy 展开 BeachItem 看不到 ART_LevelUpFx → 右键 Override Apply All。
  - R4（中）：Animator m_HasTransformHierarchy:1 + m_KeepAnimatorStateOnDisable:0 + Image m_Sprite 空 → 播放前 Image 空白（理论上 Play(0,0,0)+Update(0) 立即上 first sprite）。
- 排查步骤（按顺序）：
  1. scene Hierarchy 展开任一 BeachItem 看是否有 ART_LevelUpFx 子节点
  2. 抽到 2 个同款同等级基础单位，拖动合并，看 BoardRecruitDestination:914 的 Debug.Log 是否输出
  3. 合并触发后，surviving view GameObject name 是 "BeachItem" 还是 "Card_<id>" → 区分 R1（board）vs bench
  4. surviving view 下 ART_LevelUpFx activeSelf 是否被 SetActive(true)
- 若 Step 4 activeSelf 仍 false → 临时在 PlayLevelUpFx()(:526) 加 Debug.Log 区分 resolve=true/false 与 StartCoroutine 是否生效。
- 关键文件：DraggableUnitView.cs:526/542、BoardRecruitDestination.cs:874-918、GreyboxBoardView.cs:1806/1812、UiNodeBindingMap.cs:74-108。

## Merchant ConfirmBtn 删除物品无效根因（2026-09-22 分析，未改代码）
- 现象：Main 场景 MerchantPanel/Bg/CancleItemPanel/ConfirmBtn 点击后，MyItemBg 里的物品不消失。
- 根因：**Go 模式下 `GoMerchantGateway.RemoveInventoryItemAsync` 是空壳 stub**（`GoServerInventoryGateways.cs:610-621`）：不调任何服务端接口，直接返回 `Removed=false` + 重新 GET `/v1/items` 的原背包。`MainMerchantController.OnConfirmDeleteClicked`(:1118) 无条件用返回 Inventory 重刷（`PopulateOwnedItems`）→ 物品原样回来，且不看 `result.Removed`、不提示，面板静默关闭，表现即"点了没反应"。
- 佐证：客户端全部 Go 网关无任何物品删除接口（`"DELETE"` 仅 push devices / social link / hero rune）；`ClientServiceConfig.asset backendMode: 1`（GoUnary）→ 该 stub 正是被激活的实现。Local 模式 `LocalMerchantGateway.cs:288-326` 真删（OwnedProductIds + PlayerPrefs 幂等键），功能是通的。
- 修复方向：①服务端补删除接口（后端 PENDING 类问题）；②客户端兜底：Go 网关对 loadout 内物品可走 `SetItemLoadoutAsync` 移除，或 `Removed=false` 时 ShowTip("Unavailable") 且不关面板；③Controller 应检查 `result.Removed`。

## Bench 升级 FX 不播放 - 三行 Debug 定位法（2026-09-22）
- 在 DraggableUnitView.PlayLevelUpFx() 顶部加 1 行 Debug.Log 打印 viewName + transformPath + hasFxNode + animator:
- viewName=BeachItem hasFxNode=True → 代码通了，是视觉问题
- viewName=Card_xxx hasFxNode=False → target 在 board 上而非 bench（GreyboxBoardView:780 Instantiate(unitPrefab) 没 ART_LevelUpFx 子节点）
- 看不到 Debug 行 → HandleBasicUnitMerged 没触发 = 没真正合并（不是同 ConfigId 同 Level / target 是空格）
- 关键代码：GreyboxBoardView.cs:767-783 RefreshUnitsCore 决定 surviving view 来源 —— bench 复用 beachItemViews，非 bench 新 Instantiate unitPrefab
- 辅助方法 GetTransformPath(Transform) 用于打印完整层级路径

## Bench 升级 FX 排查第二轮：日志已出，静态全绿，收敛到 Resolve 闸门（2026-09-22）
- 日志（3 次合并，均 hasFxNode=True、animator=ART_LevelUpFx）：viewName = `Card_ai_r01.s03` / `BeachItem` / `Card_ai_r01.s04`。
- **上一轮 R1 假设作废**：board 卡走的 `UnitCard.prefab` 也挂了 ART_LevelUpFx（`Content/UI/Prefabs/Components/UnitCard.prefab:342`），不是"只有 BeachItem 有"。
- 已静态排除（全部正常，均有实证）：
  - 节点：BeachItem.prefab:150-155 / UnitCard.prefab:342-347，m_IsActive:0
  - Animator：两处 `m_Controller: {fileID: 9100000, guid: 4f5485ea83e873f4bb1efb20745dff74, type: 2}`，与可用样板 `RuneboltMage.prefab:675` 写法完全一致
  - meta：`UnitLevelUp.controller.meta` = `mainObjectFileID: 9100000`（全项目 controller 一致，勿被 `.anim.meta` 的 7400000 误导）
  - controller 结构：与可用的 `boomBoard.controller` / `LevelUp.controller` 逐字段一致；`m_DefaultWeight: 0` 在团结 2022.3.62t14 是正常值（AXE/Enemy01 等可用的也是 0）
  - clip：`m_PPtrCurves` 7 帧 `attribute: m_Sprite`、`path:` 空（= Animator 所在节点自身的 Image）、`classID: 114`、script guid `fe87c0e1cc204ed48ad3b37840f39efc` 与 prefab Image 的 m_Script 一致 → 绑定正确
  - sprite：7 张均 184×184 RGBA 真实 PNG（`V2/Content/Resources/UIResources/Unit/LevelUp/`）
  - 渲染序：ART_LevelUpFx 是 BeachItem 根**最后一个**子节点 → 在最上层
  - 场景：`Greybox_Main.unity` 内无任何 LevelUpFx / controller guid 覆盖
  - **BeachItem.prefab 无 CanvasGroup**（UnitCard 仅 root 有 1 个）→ bench 卡不会被 alpha=0 吃掉
- **推论**：bench 上节点一旦被激活就必然可见；既然"始终隐藏"→ `ResolveLevelUpFx()` 恒 false。两条可能：①运行时 `levelUpAnimator.runtimeAnimatorController == null`；②`levelUpFxResolved` 闩锁（DraggableUnitView.cs:579-582）在更早一次失败后被永久置 true，导致该 view 实例此后永远 no-op。
- 时序：`ResolveLevelUpFx` 先 `SetActive(false)`(:601)，`RunLevelUpFx` 再 `SetActive(true)`(:608) → 若 resolve 返回 false 则**根本没有 SetActive(true)**，永远停在 prefab 的 m_IsActive:0。
- 可靠时长：clip 0.1167 / state speed 0.2 = 0.583s → clamp 到 `LevelUpFxAuthoredSeconds=0.6f`，循环 `normalizedTime>=1` 提前 break → 约 0.58s 可见窗口。
- 下一步：Debug 移到 `ResolveLevelUpFx()` **之后**打印 controller 名 + resolved + latched；同时清掉 Console 过滤（右上角 134 errors 被 `PlayLevelUpFx` 过滤遮住）。
- 加固建议：`levelUpFxResolved` 一次性闩锁会永久砖掉一个 view 实例，应改为每次重试；`FindUi` 未命中应留可诊断日志。

## Bench 升级 FX 第三轮：日志全绿 → 收敛到「只播 0.58s」+ 运行时 overlay 排序（2026-09-22）
- 用户实测日志（Debug 移到 Resolve 之后）：3 次合并全部 `node=True anim=True ctrl=UnitLevelUp resolved=True imgEnabled=True sprite=微信图片_..._297_2 stateLen=0.5833 inHierarchy=True`，latched=False/True 均有。
- **上一轮推论作废**：节点在合并瞬间**确实**被 SetActive(true) 且 Image 已拿到 sprite（`RunLevelUpFx` 的 Play+Update(0f) 生效）。可见窗口只有 ~0.583s（≈35 帧 @60fps）→ Hierarchy 里不 Pause 几乎抓不到 → 之前"始终保持隐藏"是采样假象。
- 静态审计新增证据（全部通过）：
  - 7 张 sprite 是真特效：184x184 RGBA，alphaMax 255，覆盖 20~41%，bbox≈150x140 → 不是"图本身透明"
  - `UnitLevelUp.anim` = 7 帧 m_Sprite swap（time 0~0.1，sampleRate 60），path 空 = Animator 自身节点，classID 114
  - BeachItem fx Animator：`m_CullingMode:0`(AlwaysAnimate) / `m_UpdateMode:0` / controller 有效，与项目内可用的攻击动画 Animator 同构
  - `LevelUpFxAuthoredSeconds=0.6` / `LevelUpFxMaxSeconds=2`（DraggableUnitView:81-82）；duration=Clamp(0.5833/(0.2*1),0.6,2)=2.0，loop 在 normalizedTime>=1 时 ~0.583s 退出
  - 场景与运行时都无 RectMask2D；`timeScale` 仅在暂停/Boss 警告时置 0（GreyboxHudView:454/488）→ 合并时正常
  - BeachItem 根 RectTransform = anchor(0.5,0.5)/pos(0,0)/size 100×100；fx = anchor(0.5,0)/pos(0,60)/size 120×120，位置合理
- **结构性发现（子节点顺序数学与日志完全吻合）**：`DraggableUnitView.ResolveSoulChainOverlay()`(:779-809) 运行时 new `ART_SoulChainOverlay` 并 `SetAsLastSibling()` → 永远画在最上层。
  - BeachItem（prefab 3 子：Image/Text/fx）+ `InputReceiver`(SetAsFirstSibling http://:222) + overlay(末位) = 5 子，fx = **index 3/5** ✓ 与日志一致 → bench 卡的 fx 上面确实压着 overlay
  - UnitCard（prefab 4 子：ART_SoulChainOverlay/HeroLevelLabel/ART_UnitPortrait/fx，overlay 在 fx **之前**）+ InputReceiver = 5 子，fx = **index 4/5** ✓ 与日志一致
  - overlay 默认 alpha=0（`SetSoulChainControlled(false)` → `SetSoulChainOverlayAlpha(0)` :709）→ 正常态不遮挡；仅 soul-chain 控制 / bloodcrown 压制时 alpha = `SoulChainDarkAlpha` 才盖住
  - bench 卡**没有** CanvasGroup → `ApplyVisibilityToCanvasGroup`(:251-263) 对 bench 完全无效（`SetDeploymentVisualHidden` 只在有 CanvasGroup 的 UnitCard 上生效）
- 关键代码：`PlayLevelUpFx`(:553) / `ResolveLevelUpFx`(:577，一次性闩锁) / `RunLevelUpFx`(:610) / `HideLevelUpFx`(:669) / `ResolveSoulChainOverlay`(:779) / `EnsureInputReceiver`(:190，SetAsFirstSibling :222)
- 待用户验证（3 选 1 即可定性）：① Animator 里把 UnitLevelUp state Speed 0.2→0.02（≈5.8s）看是否只是太快 ② prefab fx `m_IsActive: 0→1` 看静态帧是否可见（隔离资产渲染）③ Play 中合并瞬间按 Pause 抓帧看 activeSelf + Image.sprite
- 建议加固：`RunLevelUpFx` 里 `SetActive(true)` 之后加 `animator.transform.SetAsLastSibling()`，避免 bench 上被运行时创建的 `ART_SoulChainOverlay` 覆盖。

## ★ 根因定案：CompleteDrag 二次 RefreshUnits 把 fx 掐死（2026-09-22，bench 升级 FX）
用户新观测：prefab 里把 ART_LevelUpFx 设为 visible → 动画能播（其实是 Animator 在视图激活时播默认 state，与协程无关，不能证明合并路径正常）；hidden 时合并升级 fx 不自动变可见。
- 确定性根因链（bench 卡 100% 命中）：`GreyboxBoardView.CompleteDrag`(:988)
  1. `:1043 status = drag.Drop(target)` → `Grid/DragPlacementController.Drop`(:131) → `ResolveDrop`(:148) → `occupiedDropResolver.ResolveOccupiedDrop`(:173) → `BoardRecruitDestination.cs:916 BasicUnitMerged?.Invoke`（**同步**）→ `GreyboxBoardView.HandleBasicUnitMerged`(:1806) → `RefreshUnits()`(:1808) + `PlayBasicUnitLevelUpFx(targetId)`(:1809) → `DraggableUnitView.PlayLevelUpFx`(:553) → `RunLevelUpFx`(:610) 首帧同步 `SetActive(true)+Rebind+Play(0,0,0)+Update(0)`(:613-616) → **fx 此刻真的显示了**（=用户日志 [FX]/[FX-RUN] 全绿那一刻），协程挂起在 `yield return null`(:634)
  2. 回到 CompleteDrag → **`:1051 RefreshUnits()` 第二次刷新** → `RefreshUnitsCore`(:734) **`:750-756 foreach (authoredBeachItemViews) view.gameObject.SetActive(false)`** → `DraggableUnitView.OnDisable`(:1090) → **`:1093-1097 StopCoroutine + levelUpFxCoroutine=null + HideLevelUpFx()`** → `levelUpAnimator.gameObject.SetActive(false)`(:669-675) → fx 被关掉
  3. `:791 unitView.gameObject.SetActive(true)` 只恢复**视图根**；`ART_LevelUpFx` 是独立子节点，被 HideLevelUpFx 关掉后**无人再打开**且协程已死 → 永久 inactive = 用户看到的"不会自动变为显示状态"
- 为什么"日志全绿却没效果"不矛盾：日志打在第 ① 步的瞬间，第 ② 步在同一帧把它杀掉了。
- 为什么 board 上的 `Card_*` 视图可能不受影响：它们不在 `authoredBeachItemViews` 里（只有场景授权的 5 个 bench BeachItem 被 :2354-2355 登记），:754 不会关它们 → bench 专属 bug，正好对应 D 的需求场景。
- **修复方案 1（根治，改 DraggableUnitView.cs）**：新增 `levelUpFxInterrupted`；`OnDisable` 里**删掉 `HideLevelUpFx()`**、改为 `levelUpFxInterrupted=true` + 停协程；新增 `OnEnable()`：标记为真则 `PlayLevelUpFx()` 重播；`Initialize()`(:154) 里清标记（避免视图换绑新单位时误播）。
- **修复方案 2（可选叠加，改 GreyboxBoardView.cs）**：`HandleBasicUnitMerged`/`HandleBasicUnitLevelChanged` 改为 `QueueBasicUnitLevelUpFx`（记入 `pendingLevelUpFxIds`），在 `LateUpdate`(:1689) 里 `FlushPendingUnitLevelUpFx()`，即等本帧所有 RefreshUnits 结束再播。
- 另需清理：`PlayLevelUpFx`(:555-561) 的 `[FX]` 与 `RunLevelUpFx`(:617-622) 的 `[FX-RUN]` 临时 Debug.Log。

## V1 影响判定 + 已实施修复的缺陷（2026-09-22 复核）
- 用户已手动实施方案 1（DraggableUnitView.cs 落盘，行号已位移 ≈ -36），未实施方案 2。
- **V1 影响：无。实证：** 全项目含 `ART_LevelUpFx` 的资产只有 3 个（DraggableUnitView.cs + **V2**/BeachItem.prefab + **V2**/UnitCard.prefab）；V1 的 BeachItem.prefab（子节点只有 BeachItem/Image/Text）与 V1 UnitCard.prefab（UnitCard/ART_SoulChainOverlay/HeroLevelLabel/ART_UnitPortrait）命中数均为 0；`UnitLevelUp.controller` guid 在 V1 prefab 引用数 0。
  → V1 走 `ResolveLevelUpFx` → `FindUi("ART_LevelUpFx")`==null → return false → `PlayLevelUpFx` 直接 return → `levelUpFxCoroutine` 恒 null → OnDisable/OnEnable 新分支永不进入。`CreateDragGhost` 的 `ghost.HideLevelUpFx()` 未动，幽灵行为两版一致。`levelUpFxInterrupted` 是私有非序列化 bool，不会进 V1 prefab/scene。
- **★ 已实施的修复不完整（P0）**：`levelUpFxInterrupted` 全文件仅 4 处（:82 声明 / :151 =false / :1074 if(!..) / :1079 =false），**从未被赋值 true**。`OnDisable`(:1054-1061) 删掉了 `HideLevelUpFx()` 但漏了 `levelUpFxInterrupted = true;` → ① OnEnable 恒 early-return，根因未消除；② fx 节点 activeSelf 停在 true + Animator `m_KeepAnimatorStateOnDisable:0` 在视图重启时自动重播默认 state，却无协程收尾 → 末帧爆闪 sprite 会永久留在卡面。补 `levelUpFxInterrupted = true;` 即完整。
- 建议追加：`Initialize()`(:149-157) 里在 `levelUpFxInterrupted = false;` 后补一次 `HideLevelUpFx();`，覆盖"特效播放中视图长时间不恢复（bench 槽空置后由新单位占用）"的幽灵闪光（V1 下 `levelUpAnimator==null` 直接 return，仍零影响）。

## LoadingPanel 头像挂错位置 + 批量 UI binding ambiguous（2026-09-22 定位完成，未改代码）
- 现象：V1 运行时 ProfileImg 实例挂到 `MyPart/MyItem/Image`（579×93 名条底图）而非 V1 应有的 `MyPart/Image`（255×255 头像位）；Console 刷 8 条 `UI binding ... is ambiguous below 'LoadingPanel'`。
- **根因（确定性；由提交 5ea5ada「feat（V2）：新增游戏加载、结算、暂停页面及相关逻辑」引入）**：
  - `Scripts/UI/PlayerAvatarView.cs:165-176` PlayerAvatarSceneInstaller 的候选路径被改成 **V2 优先**：`BG/MyPart/MyItem/Image` → `MyPart/MyItem/Image` → `BG/Image` → `BG/MyPart/Image`（V1 真位被挤到第 4）。改前只有 `BG/Image` ?? `BG/MyPart/Image`。
  - V1 场景 `Variants/V1/Scenes/Greybox_Main.unity` 的 LoadingPanel **恰好也存在** `BG/MyPart/MyItem/Image`（那是名条底图，里面挂 Rank）→ `Transform.Find` 精确命中 → 静默挂错、零报错。
  - 实测尺寸（解析 YAML）：V1 `MyPart/Image`=255×255、`EnemyPart/Image`=255×255、`MyItem/Image`=579×93；V2 `MyPart/MyItem/Image`=200×200、`EnemyPart/EnemyItem/Image`=200×200 → V2 候选只对 V2 成立。
  - 副作用：`Mount` 里 `slotImage.enabled=false` 会关掉被误选的 `MyItem/Image` 底图；`FitCompletePrefab` 按 min(slot.w,slot.h) 缩放 → 头像被压到 ~93px。
- **8 条 ambiguous 全部对上 V1 布局**（`UiNodeBindingMap.cs:87-105` 第三级叶子名兜底：精确路径失败 → 按叶子名搜全子树 → >1 命中即 LogError 并 return null）：
  - `GameplayLoadingPanelController.cs:229-245` 的 V2 形状路径在 V1 全部 miss：`MyPart/MyItem/Rate/RateText` ×2、`EnemyPart/EnemyItem/Rate/RateText` ×2（叶子 RateText 有 2 个）、`MyPart/MyItem/TextBg/Rank` ×2（叶子 Rank 有 2 个：`EnemyItem/TextBg/Rank` + `MyItem/Image/Rank`）。
  - `PlayerAvatarView.cs:174-175` 的 `BG/EnemyPart/EnemyItem/Image`、`EnemyPart/EnemyItem/Image` ×2（叶子 Image 有 3 个）→ null → 落到第 3 候选 `BG/EnemyPart/Image` 才命中（所以敌方头像看起来正常，只有玩家侧错）。
  - 玩家侧候选 1 精确命中 → 不报错；候选 3 压根没被求值 → 所以没有 `BG/Image is ambiguous` 这条，与截图 8 条完全吻合。
- V1 场景自身命名不对称（同一块名条：玩家侧名为 `MyItem/Image`、敌方侧名为 `EnemyItem/TextBg`），是让 V2 优先查找"半对半错"的帮凶。
- 资产侧无问题：V1/V2 各有独立 ProfileImg.prefab（guid 668ed60f… / d7c8053d…），注册表 key `prefabs/ProfileImg` 均在；场景/prefab 内**无任何 UiNodeBindingMap 显式绑定**（`grep -rl UiNodeBindingMap --include=*.unity --include=*.prefab` 为空）→ FindUi 永远走兜底。
- 修复方向（供后续选，本次未改）：A 候选列表按激活变体分流（V1 只查 `BG/MyPart/Image`、`BG/EnemyPart/Image`）；B 给 LoadingPanel 加 `UiNodeBindingMap` 显式绑定语义键（顺带消掉 8 条 error）；C 改 V1 场景重名节点（牵动其它引用，风险最高）。
- 复用工具：`TempArchiveCompare_20260918/dump_hierarchy.py`（解析 .unity/.prefab 的 GO/Transform 块还原层级）、`dump_rect.py`（带 sizeDelta/anchoredPosition/组件）。

## LoadingPanel 修复方案（2026-09-22 输出，用户手动改，未改代码）
- 变体判定两法：① `scene.path` 含 "/Variants/V2/"（直接、无依赖，编辑器+构建都成立）；② `UiAssets.Active?.VariantId`（编辑器下 TryActivateEditorRegistry 读 PlayerSettings.preloadedAssets，时机上可能为 null）。推荐 ① 为主 + ② 兜底。
- V1 真实节点树（dump_hierarchy.py 实解 `Variants/V1/Scenes/Greybox_Main.unity`）：
  LoadingPanel/BG/{BgShadow, Title, EnemyPart/{Image(255x255), EnemyItem/{TextBg/Rank, Text (TMP)/RateText}}, MyPart/{Image(255x255), MyItem/{Image(579x93)含 Rank, Text (TMP)/RateText}}, Device}
- V2 真实节点树（`Variants/V2/Scenes/Greybox_Main.unity`）：
  LoadingPanel/BG/{Title, Device, EnemyPart/EnemyItem/{Image(200x200), TextBg/Rank, Rate/RateText}, MyPart/MyItem/{Image(200x200), TextBg/Rank, Rate/RateText}}
- P0（必修，`Scripts/UI/PlayerAvatarView.cs:163-182` PlayerAvatarSceneInstaller.OnSceneLoaded）：候选按变体分流。V2 = `BG/MyPart/MyItem/Image` + `BG/EnemyPart/EnemyItem/Image`；V1 = `BG/MyPart/Image`(→`MyPart/Image`) + `BG/EnemyPart/Image`(→`EnemyPart/Image`)。**必须删掉 V1 下的 MyItem/Image 兜底**，否则仍会挂到名条。
- P1（治噪，`Scripts/UI/GameplayLoadingPanelController.cs:229-245` ResolvePresentationTexts）：同样分流。V1 rate=`BG/MyPart/MyItem/Text (TMP)/RateText` / `BG/EnemyPart/EnemyItem/Text (TMP)/RateText`；V1 rank 玩家=`BG/MyPart/MyItem/Image/Rank`、敌方=`BG/EnemyPart/EnemyItem/TextBg/Rank`（V1 两个 rank 其实都在，只是路径不对称；旧代码 aiRankText 已命中→V1 现在是"只填敌方"的半填充态）。V2 rate=`.../Rate/RateText`、rank=`.../TextBg/Rank`。
- P2（架构级可选）：给 V1/V2 两个场景的 LoadingPanel 挂 `UiNodeBindingMap`，语义键 player-avatar-slot / enemy-avatar-slot / player-rate-text / enemy-rate-text / player-rank-text / enemy-rank-text。FindUi 第一级 `GetComponentsInParent<UiNodeBindingMap>(true)` 即命中 → 8 条 error 与路径猜测同时消失。Entry.key/target 是私有 [SerializeField]，可在 Inspector 直接填。
- 修复正向副作用：V1 头像槽从 579x93 名条 → 255x255 真位；`Mount()` 的 `slotImage.enabled=false` 不再误关名条底图；`FitCompletePrefab` 改按 min(255,255) 缩放。
- 已输出逐行手动修改步骤（改 2 个 .cs，纯空格缩进 + LF 行尾，无需改 prefab/scene）：
  - `Scripts/UI/PlayerAvatarView.cs`：替换 163-182；新增 `IsV2Variant(Scene)` 私有静态方法，插到 `OnSceneLoaded` 之后、`FindDescendant`（原 186 行）之前。
  - `Scripts/UI/GameplayLoadingPanelController.cs`：替换 228-245；新增 `IsV2Variant(Scene)`，插到 `ResolvePresentationTexts` 之后、`RefreshRankTexts`（原 255 行）之前。
  - 注意：`GameplayLoadingPanelController.cs` 无 `using System;` → StringComparison 用全限定 `System.StringComparison.OrdinalIgnoreCase`；`aiRankText` 两变体路径相同（`BG/EnemyPart/EnemyItem/TextBg/Rank`）故不用分流。
  - 范围确认：V1/V2 场景中只有 `Greybox_Main` 含 LoadingPanel（Main/Login 都没有；`V1/Scenes/Game.unity` 有但未进 Build Settings，是遗留场景）。


## V1 头像「不再跟随出场动画」根因（2026-09-22，只分析未改）
- 现象：头像挂载位置修正后（V1 挂到 BG/MyPart/Image），玩家/AI 头像不再跟 LoadingPanel 出场动画一起飞入。
- 根因（结构差异，实证）：出场动画 LoadingPanelEntranceAnimator（挂在 Canvas/LoadingPanel，场景序列化 entranceMode=1=Horizontal、duration=0.6、horizontalPlayerDelay=0.05、playOnEnable=1）只位移 CaptureTargets()/AddPartTargets() 解析出的两个节点：BG/EnemyPart/EnemyItem 与 BG/MyPart/MyItem（LoadingPanelEntranceAnimator.cs:161-189）。
  - V1 场景：头像槽 MyPart/Image(255x255，绝对 y[-773.5,-518.5]) 与 EnemyPart/Image(255x255，y[428.5,683.5]) 是 MyItem(y[-452,-148])/EnemyItem(y[92.8,397.2]) 的兄弟节点 → 不在位移集合 → 不跟随。
  - V2 场景：头像槽在 Item 内部（MyItem/Image 200x200 y[-574,-374]、EnemyItem/Image y[48.5,248.5]）→ 天然跟随。
- 回归来源：修复前 V1 玩家头像走候选1 BG/MyPart/MyItem/Image（在 Item 内，会跟随出场）；修复后改挂 BG/MyPart/Image（Item 外，不跟随）。敌方头像两版都落在 EnemyPart/Image（一直在外面）。
- 已排除：头像未被遮挡/隐藏。按 anchor+pivot 算绝对矩形，V1 头像槽与其 Item 矩形不重叠；LoadingPanel 子树内无 CanvasGroup/Mask/RectMask2D；出场水平位移 distance=panelWidth+max(extent)≈±2072（1080 宽画布）→ 起始整体离屏，头像则第 0 帧就在终点位。
- 布局基准：画布 1080x1920；V1/V2 一致 —— MyPart=下半屏(y[-960,0]，玩家)，EnemyPart=上半屏(y[0,960]，敌方)。
- 附带发现：两个场景里的 LoadingPanel 都是场景自带对象（GO 的 m_PrefabInstance: 0），而注册表 prefabs/LoadingPanel（V1 guid b259469…/V2 guid ddfb4683…）是另一套更旧形状（无 BG、无出场动画组件、WeaCon 在根下）→ 改层级/动画必须改场景，改 prefab 无效。
- 修复方向（供选，未执行）：A AddPartTargets 里用 part.Find("Image") 追加槽位到 targets，且仅当 slot 不是 item 后代（否则 V2 双重位移）；B 把 V1 两个槽移进 Item 下（须同步改挂载路径为 BG/MyPart/MyItem/Image，重排 anchor）。
- 工具：TempArchiveCompare_20260918/dump_loading_panel.py（层级+active+组件+prefab 来源）、dump_abs_rect.py（按 anchorMin/Max+pivot+sizeDelta 算绝对矩形）。

## 出场动画跟随修复：方案 A 落地步骤（已输出，用户手动改，未落盘）
- 目标文件唯一：`Assets/Scripts/UI/LoadingPanelEntranceAnimator.cs`（V1/V2 共用一份，无变体分支，靠结构自适配）。
- 改点 1：`CaptureTargets()` 第 168-169 行两处调用追加第 3 个实参 `"Image"`。
- 改点 2：`AddPartTargets`（第 173-182 行）加 `string avatarSlotName` 形参；`AddTarget(item, targets)` 之后追加槽位，用 `part.Find(avatarSlotName)` + `avatarSlot.IsChildOf(item)` 早退。
- **核心坑（必须在方案里警示）**：只能 `part.Find(...)`（Transform.Find，仅直接子节点），**禁止用 `part.FindUi("Image")`** —— V1 的 MyPart 子树里 `Image` 出现 2 次（`MyPart/Image` 255×255 头像槽 + `MyPart/MyItem/Image` 579×93 名条底图）→ FindUi 叶子名兜底判歧义 → LogError 且返回 null → 静默失效。V2 下 `Find` 返回 null（槽在 Item 内）→ 天然不添加 → 无双重位移。
- 冗余守卫 `isChildOf` 保留，防止日后有人换成 FindUi 造成 V2 双重位移。
- 副作用已核对：新增 255×255 target 只抬高 `GetLargestExtent`，而 Horizontal 偏移 = panelWidth + max(extent) ≈ 2072 已远大于画布 1080 → 无视觉差；`RestoreFinalPositions` 覆盖新 target，退出时 anchoredPosition 复原，不留布局污染。
- 回滚：删两处 `"Image"` 实参即恢复原行为。

## V2 招募区升级动画 ART_LevelUpFx：17:10 复核现状（回答"你还记得吗"）
- 资产侧 `V2/Content/Resources/prefabs/BeachItem.prefab` 已被用户微调（旧记录 120×120/y60 作废）：ART_LevelUpFx GO `4332711986383810658` / RT `8465729890412022266` → anchor **(0.5,0)**、anchoredPosition **(0,70)**、sizeDelta **140×140**、pivot 0.5/0.5、m_IsActive **0**；根卡 `9198921445983142549` 为 100×100（anchor 0.5/0.5）→ fx 覆盖卡面并上溢 40px、左右各溢 20px。Image raycastTarget 已改 **0**、sprite 空；Animator controller=UnitLevelUp guid `4f5485ea83e873f4bb1efb20745dff74`、m_CullingMode 0、**m_KeepAnimatorStateOnDisable 0**；根 m_Children 顺序 [Image, Text, ART_LevelUpFx]（fx 最上层）✓。
- 代码侧 `Assets/DragonBound/Runtime/Presentation/DraggableUnitView.cs` **修复仍不完整**：`levelUpFxInterrupted` 仅 :82 声明 / :151 =false / :1074 读 / :1079 =false，**从未置 true** → OnEnable(:1072) 恒 early-return；OnDisable(:1054) 已无 HideLevelUpFx 但也没置位 → fx 节点停在 activeSelf=true，Animator 因 keepState=0 会在视图重新启用时自播一次默认 state 且无人收尾 → 末帧 sprite 残留。**补 `levelUpFxInterrupted = true;` 即闭环**。
- 当前行号（文件已位移，旧记录 526/553 作废）：PlayLevelUpFx :531、ResolveLevelUpFx :547、RunLevelUpFx :580、HideLevelUpFx :633（调用点 :601 收尾 / :939 幽灵）、OnDisable :1054、OnEnable :1072；GreyboxBoardView：订阅 :569-570、HandleBasicUnitLevelChanged :1800、HandleBasicUnitMerged :1806、PlayBasicUnitLevelUpFx :1812、CompleteDrag :988 →（Drop 后）RefreshUnits :1051。临时 `[FX]`/`[FX-RUN]` Debug.Log 已清（仅剩 :1149 无关 LogWarning）。

## V2 bench 升级 FX「播完过一会儿又播一次」根因 + 修复方案（17:18，未执行）
- 现象：合并后 fx 播完，稍后（下一次任意刷新时）又自动播一次。
- 根因（双机制叠加）：
  1. **fx 节点被永久留在 `activeSelf=true`**。合并帧：`BoardRecruitDestination:916` 同步 `BasicUnitMerged` → `GreyboxBoardView.HandleBasicUnitMerged(:1806)` 先 RefreshUnits(:1808) 再 `PlayBasicUnitLevelUpFx`(:1812→:1809) → `DraggableUnitView.PlayLevelUpFx`(:531) → `RunLevelUpFx`(:580) 首帧 SetActive(true)+Rebind+Play(0,0,0)，协程挂起在 :598；**同一帧稍后** `CompleteDrag:1051` 又 RefreshUnits → `RefreshUnitsCore:750-756` 把全部 authored beach item 视图 SetActive(false) → `OnDisable:1054` StopCoroutine（协程死 → `RunLevelUpFx:601` 的 `HideLevelUpFx()` 永不执行）→ :791 再 SetActive(true) → `OnEnable:1072` 因 `levelUpFxInterrupted` 恒 false 直接 return（不补播）；fx 子节点 activeSelf 仍为 true（Unity 不会因父节点停用而改子节点 activeSelf）。
  2. **Animator 后门**：prefab `ART_LevelUpFx` 的 `m_KeepAnimatorStateOnDisable: 0` → Animator 随 GO 经历一次"停用→启用"就丢弃状态、从头自动重播默认 state（0.583s），且无协程收尾 → 停在末帧（alpha≈67/255）。而 `RefreshUnitsCore:750-756`+`:791` 这对 SetActive **每轮 RefreshUnits 都会执行**（AI 行动 :531/:277、wave 推进 :1685/:1708、下次拖拽 :1051）→ 每刷新一次就可能自动再播一次 = 用户看到的"过一会儿又播"，并留末帧淡影。
- 方案 A（根因，`GreyboxBoardView.cs`）：复用文件已有的 `pending*` + 帧末消费惯用法（参考 `pendingSynthesisPairIds`：:77 声明 / :1705 入队 / :1883 在 `RefreshPairPresentations` 消费 / :1696 `LateUpdate` 调用）→ 新增 `pendingLevelUpFxIds`；`HandleBasicUnitLevelChanged:1803`/`HandleBasicUnitMerged:1809` 改为只入队；`LateUpdate:1689` 末尾 `FlushPendingBasicUnitLevelUpFx()` 统一播 → fx 在本帧所有 RefreshUnits 之后才播，不再被同帧掐断，协程正常 `HideLevelUpFx()` 归位。
- 方案 B（兜底，`DraggableUnitView.cs`，必做）：`OnDisable` 里先判 `levelUpFxCoroutine != null` → 置 `levelUpFxInterrupted = true` 再 Stop，随后补 `HideLevelUpFx()`；`OnEnable:1072` 保留补播分支；`Initialize:151` 的 `= false` 后补一次 `HideLevelUpFx()`。
- 方案 C（资产加固，2 处各 1 行）：V2 的 `prefabs/BeachItem.prefab` 与 `Content/UI/Prefabs/Components/UnitCard.prefab` 里 Animator 的 `m_KeepAnimatorStateOnDisable: 0 → 1`，关掉"启用瞬间自动重播"后门。单做 C 只消除重复播放，仍会留末帧残留，故只能当加固。
- 反直觉结论：当前"半截修复"把两条路都走歪 —— 该补播时分支永不进入（标记从未置 true），不该播时 Animator 自己播（keepState=0）。
- **方案 A 逐行手动步骤已输出（17:33，用户手动改）**，锚点（行号均为改前）：
  1. `GreyboxBoardView.cs` 字段区 :79-80 之后追加 `private readonly HashSet<string> pendingBasicUnitLevelUpIds = new HashSet<string>(StringComparer.Ordinal);`（**用 HashSet 不用 List** —— `List<T>` 没有 comparer 构造，照抄 `new List<string>(StringComparer.Ordinal)` 会编译报错；HashSet 同时与 :77-78 邻居风格一致并天然去重）。
  2. `HandleBasicUnitLevelChanged:1803` / `HandleBasicUnitMerged:1809` 的 `PlayBasicUnitLevelUpFx(...)` → `QueueBasicUnitLevelUpFx(...)`。
  3. 在 `PlayBasicUnitLevelUpFx`(:1812-1824) 之后、`HandleHeroPairUnlinked`(:1826) 之前插入 `QueueBasicUnitLevelUpFx(string)`（空/空白 id 直接 return + Add）与 `FlushPendingBasicUnitLevelUpFx()`（Count==0 早退 → foreach 调 `PlayBasicUnitLevelUpFx` → Clear；`PlayBasicUnitLevelUpFx` 不改集合，迭代中安全）。
  4. `LateUpdate:1689-1697` 末尾（`RefreshPairPresentations();` 之后）追加 `FlushPendingBasicUnitLevelUpFx();`。注意 :1691-1694 的早退不覆盖它（`unitDestination==null` 时集合必然为空，无副作用；想更严谨可把早退改成 `if (unitDestination != null && board != null) RefreshPairPresentations();` 再无条件 flush）。
  5. 仍建议配套方案 B 的 `levelUpFxInterrupted = true;`：A 只消除"合并帧同帧打断"这条必然路径；若 0.6s 播放期内又有一次 RefreshUnits（立刻再拖一张卡 / AI 回合）仍会掐断 → 依旧残留 + 自动重播。
- 佐证/先例：`RefreshPairPresentations`(:1850-1905) 不调用 RefreshUnits，且 :1883-1894 已在 LateUpdate 驱动的刷新里播 `PlaySynthesisAnimation`/`PlayLevelUpAnimation` —— 本文件的既有惯例就是"帧末播升级类动画"。
