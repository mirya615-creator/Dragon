# 2026-09-22

## V2 单位升级动画 UnitLevelUp 接入方案（调研，未执行）
- 资产：V2 已有 `UI/Variants/V2/Content/Resources/Animations/Unit/UnitLevelUp.anim` + `.controller`（controller 只有一个 state，speed 0.2，无参数）。
- clip = **7 帧 PPtr m_Sprite 曲线**（classID 114=Image，path 为空 → 写 Animator 所在 GO 的 Image.sprite），60fps、总时长 0.1167s、`m_LoopTime: 1`。
- 7 张帧图：`V2/Content/Resources/UIResources/Unit/LevelUp/微信图片_2026092117022{5..8}_29{7..303}_2.png`，均 184×184 RGBA；非透明 bbox 自下而上扩散(y 99→17)、alpha 峰值 255→67 → **向上扩散并淡出的叠加光效，不是替换单位图**。
- 7 个 sprite guid 全部落在 V2 目录 → 无跨变体引用，构建守卫安全。
- **V2 无 `Animation/UnitAni/*`（只有 V1 有），V2 注册表也无 UnitAni key(0)** → `BasicUnitAnimationControllerCatalog.Load()` 在 V2 返回 null → V2 单位**目前没有攻击动画**，单位 prefab 上预置的 Animator 处于空置态（enabled=false / controller=null）。
- 单位 prefab：`V2/Content/UI/Prefabs/Components/UnitCard.prefab`（guid 178d674897c9744439926a430810ea1f，V2 Greybox_Main 引用 4 次）。树：
  `UnitCard(124×124,中心锚) → ART_UnitPortrait(0,0)-(1,1), HeroLevelLabel(右上32×32), ART_SoulChainOverlay(0,0)-(1,1)`；root 挂 DraggableUnitView + CanvasGroup。
  另有 `V2/Content/Resources/prefabs/Unit.prefab`（场景引用 0 次，未被使用）。
- **触发链路**：`BoardRecruitDestination.TryIncreaseLevel`(:732) → `BasicUnitLevelChanged(Action<string> runtimeId)` → `GreyboxBoardView.HandleBasicUnitLevelChanged`(:1679) 目前只 `RefreshUnits()`。**UI 层已有订阅点，这是最佳接入位置**（比 BasicUnitMerged 更早触发，早于源单位移除）。
- 结论方案：V2 prefab 加 `ART_LevelUpFx` 子节点（Image+Animator+初始 inactive），共享代码加 null-safe 的 `PlayLevelUpFx()`；不借用 ART_UnitPortrait 的 Animator（会覆盖单位 sprite）。

## V2 单位升级动画 UnitLevelUp 接入（已执行）
- 用户已完成两步：`UnitLevelUp.anim` 的 `m_LoopTime` 改为 0；V2 `UnitCard.prefab` 新增 `ART_LevelUpFx` 子节点（RectTransform 184×184 中心锚 + Image）。
- 我补全预制体：给该节点挂 Animator（`fileID 1990000030`，`m_Controller` → UnitLevelUp.controller guid `4f5485ea83e873f4bb1efb20745dff74`）；节点改 `m_IsActive: 0`；Image `m_RaycastTarget: 0`；DraggableUnitView 序列化字段加 `levelUpAnimator: {fileID: 1990000030}`。
- `DraggableUnitView.cs`（共享代码）：新增 `[SerializeField] Animator levelUpAnimator` + `PlayLevelUpFx()` / `ResolveLevelUpFx()` / `RunLevelUpFx()` / `ResolveLevelUpFxDuration()` / `HideLevelUpFx()`；缺节点时静默返回（V1 天然无感，`FindUi` 未命中不报错）；`OnDisable` 与 `CreateDragGhost` 会停协程并隐藏节点。
- **重要链路修正**：`TryAdjustBasicUnitLevel`（压力赛升级）才走 `BasicUnitLevelChanged`；**拖拽合成升级路径 `ResolveOccupiedDrop`(:896) 直接调 `TryIncreaseLevel()`，只发 `BasicUnitMerged`**。所以 `GreyboxBoardView` 额外订阅 `BasicUnitMerged`（Initialize/OnDestroy 成对），两个 handler 都 `RefreshUnits()` 后再 `PlayBasicUnitLevelUpFx(targetUnitId)` 播光效。
- 播放时长自适应：`state.length / (stateSpeed * controllerSpeed)` 并 clamp 到 [0.6s, 2s]，另用 `normalizedTime>=1` 提前退出。
- 构建隔离安全：controller/anim/7 帧图全在 V2；V1 UnitCard.prefab 零改动、无新引用。
