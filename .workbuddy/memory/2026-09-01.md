# 2026-09-01

- 分析登录体系（Assets/Scripts/Auth/）：会话存 PlayerPrefs 键 `dragonbound.auth.session.v1`（PersistentAuthSessionStore）；游客设备身份存 `dragonbound.guest.device-id`（GuestIdentityService，32 字节随机 Base64，决定账号身份）；本地网关映射 `dragonbound.local-guest.<sha256(device_id)>` / `dragonbound.local-google.<sha256(subject)>`（LocalAuthGateway 存 GUID playerId）。**重新创建账号的关键是删 device-id**（只删 session 会用同一 device-id 登回同一账号）。
- 项目无 Logout 功能；PlayerPrefs 物理位置：Editor=注册表 HKCU\Software\Unity\UnityEditor\DefaultCompany\Dragon L1；Android=com.drakeforge.mergedefense 的 shared_prefs。
- 相关进度键（按需清理）：dragonbound.gameplay-launch.*、dragonbound.merchant.*、dragonbound.ai-recovery.*、dragonbound.runes.dev-account-day-v1、dragonbound.analytics.consent.v1、dragonbound.settings.damage-numbers-visible。
- 输出清号重建账号方案（编辑器菜单脚本 + 注册表 + adb 三途径）。
