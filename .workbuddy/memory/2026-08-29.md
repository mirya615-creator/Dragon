# 2026-08-29

- 整理 12 英雄普攻与特殊技能效果表，输出至 `Docs/英雄普攻与特殊技能效果表.md`。
- 深入分析烈焰龙骑「烈焰俯冲」：入口 `HeroCombatState.ResolveReadyDives/ResolveDive`（约 L1352-1423），燃烧地带为 `GroundHazardSystem`（Line 危害，起点=英雄原位，方向指向最前敌人，长6宽0.7，3s/1s/3跳，每跳 RuneModifiedBaseAttack×25%×技能倍率，最多4目标按PathProgress降序；同路径重复俯冲走 Refresh 不叠加）。撞击伤害用 `Attack`（非 Rune 修饰值）×200%×技能倍率。视觉：`CombatFxView.SpawnDragonRiderDive`（ART_FlameDrakeRiderDive / Resources "Animation/Flame Drake Rider S"，145×145，终点外延300px，EaseIn，飞行动画期间隐藏编队立绘，同帧去重）；灼烧 tick 走默认 attackLine 特效+飘字，目前无持续地面贴图。
- 金英雄等级倍率 GoldLevels()：攻/速/技能 = L1 1/1/1，L2 1.12/1.10/1.10，L3 1.25/1.21/1.25，L4 1.40/1.33/1.45，L5 1.57/1.46/1.70。
- 关键发现：英雄/技能/配方唯一数据源为 `Assets/DragonBound/Runtime/Recruitment/FrozenHeroConfiguration.cs`（FrozenHeroConfigurationCatalog，冻结配置+校验器）；战斗语义在 `Runtime/Combat/HeroCombatState.cs`。6 紫 + 6 金；眩晕折算：普通×1 / 精英×0.6 / Boss×0.2，Boss 眩晕后免疫 2 秒。
- 分析 SafeAreaFitter.cs；诊断 APK 顶部黑条根因（Canvas 无全屏背景，安全区外露 Camera clearColor ScreenColor）；给出 SafeAreaRoot 容器 + 全屏背景方案（Canvas 直挂 fitter 会被 Canvas Scaler 重置，不可行）。
- 梳理铲子 UI 传入链路并输出实现步骤：ComponentUI/shovel.png（已是 Sprite 导入）→ ResourcesCampComponentArtProvider 新增 TryGetShovelSprite（复用 TryLoadSprite 缓存管线）→ GreyboxBoardView.ApplyCardPresentation 加 Shovel 分支（现状落 else 纯色占位）→ DraggableUnitView.SetCardSprite → BeachItem/Image（BindBeachBenchCells L1251 动态绑定，预制体零改动）。
