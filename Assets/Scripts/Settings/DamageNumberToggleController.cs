/// <summary>
/// Legacy scene component retained so existing V2 scene serialization keeps working.
/// The Smooth control is visual-only and shares the binary slider behaviour.
/// </summary>
public sealed class DamageNumberToggleController : VisualStateToggleController
{
}
