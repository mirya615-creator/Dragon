namespace DragonBound.Presentation
{
    /// <summary>
    /// Scene-facing controller for the authored game overlay. The implementation remains in
    /// GreyboxHudView temporarily so existing prefabs and focused tests keep their serialized data.
    /// </summary>
    public sealed class GameOverlayController : GreyboxHudView
    {
    }
}
